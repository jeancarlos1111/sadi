<h2><?= htmlspecialchars($titulo) ?></h2>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="estruc_presupuestaria/index">
        <input type="text" name="search" placeholder="Buscar estructura..." value="<?= htmlspecialchars($search) ?>" style="padding: 6px; width: 260px;">
        <button type="submit" style="padding: 6px 12px;">Buscar</button>
        <a href="?route=estruc_presupuestaria/index" style="margin-left: 8px; color: gray;">Limpiar</a>
    </form>
    <a href="?route=estruc_presupuestaria/form" style="background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">+ Nueva Estructura</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th style="width: 60px;">ID</th>
            <th>Descripción de la Estructura Presupuestaria</th>
            <th style="width: 120px;">Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($estructuras)): ?>
            <tr>
                <td colspan="3" style="text-align: center; padding: 20px; color: gray;">
                    No hay estructuras presupuestarias registradas.
                    <br><a href="?route=estruc_presupuestaria/form">Crear la primera</a>
                </td>
            </tr>
        <?php else: ?>
            <?php foreach ($estructuras as $ep): ?>
            <tr>
                <td style="text-align: center; color: gray;"><?= $ep->id ?></td>
                <td><?= htmlspecialchars($ep->descripcion) ?></td>
                <td>
                    <a href="?route=estruc_presupuestaria/form&id=<?= $ep->id ?>" style="margin-right: 8px;">Editar</a>
                    <form method="POST" action="?route=estruc_presupuestaria/eliminar"
                          onsubmit="return confirm('¿Eliminar esta estructura? Las partidas presupuestarias asociadas quedarán sin estructura.');"
                          style="display:inline;">
                        <input type="hidden" name="id" value="<?= $ep->id ?>">
                        <button type="submit" style="color: red; border:none; background:none; cursor:pointer;">Eliminar</button>
                    </form>
                </td>
            </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>

<div style="margin-top: 20px;">
    <a href="?route=presupuesto/formulacion" style="color: #0056b3;">← Volver a Formulación Presupuestaria</a>
</div>
