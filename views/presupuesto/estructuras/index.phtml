<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=estruc_presupuestaria/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Nueva Estructura
            </a>
        </div>
    </div>
    <div class="card-body">
        <div class="mb-3">
            <form method="GET" action="" class="form-inline">
                <input type="hidden" name="route" value="estruc_presupuestaria/index">
                <div class="input-group input-group-sm">
                    <input type="text" name="search" class="form-control" placeholder="Buscar estructura..." value="<?= htmlspecialchars($search) ?>" style="width: 260px;">
                    <span class="input-group-append">
                        <button type="submit" class="btn btn-primary btn-flat"><i class="fas fa-search"></i></button>
                    </span>
                </div>
                <a href="?route=estruc_presupuestaria/index" class="btn btn-default btn-sm ml-2">Limpiar</a>
            </form>
        </div>

        <table class="table table-bordered table-striped dataTable w-100">
            <thead class="thead-dark">
                <tr>
                    <th style="width: 60px;">ID</th>
                    <th>Descripción de la Estructura Presupuestaria</th>
                    <th style="width: 120px;" class="text-center">Acciones</th>
                </tr>
            </thead>
    <tbody>
        <?php if (empty($estructuras)): ?>
            <tr>
                <td colspan="3" style="text-align: center; padding: 20px; color: gray;">
                    No hay estructuras presupuestarias registradas.
                    <br><a href="?route=estruc_presupuestaria/form">Crear la primera</a>
                </td>
            </tr>
        <?php else: ?>
            <?php foreach ($estructuras as $ep): ?>
            <tr>
                <td class="text-center text-muted"><?= $ep->id ?></td>
                <td class="align-middle"><?= htmlspecialchars($ep->descripcion) ?></td>
                <td class="text-center align-middle">
                    <a href="?route=estruc_presupuestaria/form&id=<?= $ep->id ?>" class="btn btn-sm btn-info" title="Editar">
                        <i class="fas fa-edit"></i>
                    </a>
                    <form method="POST" action="?route=estruc_presupuestaria/eliminar"
                          onsubmit="return confirm('¿Eliminar esta estructura? Las partidas asociadas quedarán sin estructura.');"
                          class="d-inline">
                        <input type="hidden" name="id" value="<?= $ep->id ?>">
                        <button type="submit" class="btn btn-sm btn-danger" title="Eliminar">
                            <i class="fas fa-trash"></i>
                        </button>
                    </form>
                </td>
            </tr>
            <?php endforeach; ?>
        <?php endif; ?>
        </tbody>
        </table>

        <div class="mt-4">
            <a href="?route=presupuesto/formulacion" class="btn btn-link text-primary p-0">
                <i class="fas fa-arrow-left"></i> Volver a Formulación Presupuestaria
            </a>
        </div>
    </div>
</div>
