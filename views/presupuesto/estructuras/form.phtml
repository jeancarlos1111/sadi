<div class="row">
    <div class="col-md-6 offset-md-3">
        <div class="card card-outline card-primary">
            <div class="card-header">
                <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
            </div>
            <form method="POST" action="?route=estruc_presupuestaria/guardar">
                <div class="card-body">
                    <?php if (isset($error)): ?>
                        <div class="alert alert-danger alert-dismissible">
                            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                            <h5><i class="icon fas fa-ban"></i> Error</h5>
                            <?= htmlspecialchars($error) ?>
                        </div>
                    <?php endif; ?>

                    <?php if ($estructura && $estructura->id): ?>
                        <input type="hidden" name="id" value="<?= $estructura->id ?>">
                    <?php endif; ?>

                    <div class="form-group">
                        <label>Descripción <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" name="descripcion_ep"
                            value="<?= htmlspecialchars($estructura->descripcion ?? '') ?>"
                            required maxlength="200"
                            placeholder="Ej: Dirección General - Administración">
                        <small class="form-text text-muted">Nombre completo de la unidad o programa presupuestario.</small>
                    </div>

                    <h5 class="mt-4 border-bottom pb-2 text-muted">Clasificación Programática <small>(Opcional)</small></h5>
                    <p class="text-sm text-muted mb-3">Códigos de referencia ONAPRE según el instructivo 4.</p>

                    <div class="row">
                        <div class="col-sm-4">
                            <div class="form-group">
                                <label>Acción Centralizada:</label>
                                <input type="number" class="form-control" name="id_acciones_centralizadas" min="0" value="<?= $estructura->idAccionesCentralizadas ?? 0 ?>">
                            </div>
                        </div>
                        <div class="col-sm-4">
                            <div class="form-group">
                                <label>Acción Específica:</label>
                                <input type="number" class="form-control" name="id_accion_especifica" min="0" value="<?= $estructura->idAccionEspecifica ?? 0 ?>">
                            </div>
                        </div>
                        <div class="col-sm-4">
                            <div class="form-group">
                                <label>Otras Acciones:</label>
                                <input type="number" class="form-control" name="id_otras_acciones_especificas" min="0" value="<?= $estructura->idOtrasAcciones ?? 0 ?>">
                            </div>
                        </div>
                    </div>
                </div>
                
                <div class="card-footer">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Guardar Estructura</button>
                    <a href="?route=estruc_presupuestaria/index" class="btn btn-default float-right">Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>
