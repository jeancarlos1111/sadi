<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px; border-radius: 4px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="max-width: 650px;">
    <form method="POST" action="?route=presupuesto/guardar">
        <?php if ($partida && $partida->id): ?>
            <input type="hidden" name="id" value="<?= $partida->id ?>">
        <?php endif; ?>

        <table style="width: 100%; border-collapse: collapse;">
            <tr>
                <td style="padding: 10px 0; width: 220px;"><label><b>Estructura Presupuestaria:</b></label></td>
                <td>
                    <select name="id_estruc_presupuestaria" required style="padding: 8px; width: 100%;">
                        <option value="">-- Seleccione --</option>
                        <?php foreach ($estructuras as $e): ?>
                            <option value="<?= $e['id_estruc_presupuestaria'] ?>"
                                <?= ($partida && $partida->idEstructura == $e['id_estruc_presupuestaria']) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($e['descripcion_ep']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </td>
            </tr>
            <tr>
                <td style="padding: 10px 0;"><label><b>Partida Presupuestaria:</b></label></td>
                <td>
                    <select name="id_codigo_plan_unico" required style="padding: 8px; width: 100%;">
                        <option value="">-- Seleccione --</option>
                        <?php foreach ($planUnico as $p): ?>
                            <option value="<?= $p['id_codigo_plan_unico'] ?>"
                                <?= ($partida && $partida->idPlanUnico == $p['id_codigo_plan_unico']) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($p['codigo_plan_unico']) ?> — <?= htmlspecialchars($p['denominacion']) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </td>
            </tr>
            <tr>
                <td style="padding: 10px 0;"><label><b>Monto Asignado (Bs):</b></label></td>
                <td>
                    <input type="number" name="monto_asignado" step="0.01" min="0.01" required
                        value="<?= $partida ? number_format($partida->montoAsignado, 2, '.', '') : '' ?>"
                        style="padding: 8px; width: 220px; font-size: 1.1em;">
                    <small style="color: gray; display: block; margin-top: 3px;">Solo se puede modificar el Monto Asignado original. Comprometidos y Causados se calculan automáticamente.</small>
                </td>
            </tr>
        </table>

        <?php if ($partida && $partida->id): ?>
        <div style="background: #f8f9fa; padding: 12px; border-radius: 4px; border: 1px solid #ddd; margin: 15px 0;">
            <strong>Estado de Ejecución Actual:</strong><br>
            Comprometido: <b>Bs <?= number_format($partida->montoComprometido, 2, ',', '.') ?></b> |
            Causado: <b>Bs <?= number_format($partida->montoCausado, 2, ',', '.') ?></b> |
            Pagado: <b>Bs <?= number_format($partida->montoPagado, 2, ',', '.') ?></b>
        </div>
        <?php endif; ?>

        <div style="margin-top: 20px;">
            <button type="submit" style="background-color: #007bff; color: white; padding: 10px 20px; border: none; border-radius: 4px; cursor: pointer; font-size: 1em;">
                💾 Guardar Partida
            </button>
            <a href="?route=presupuesto/formulacion" style="margin-left: 15px; color: #6c757d;">Cancelar</a>
        </div>
    </form>
</div>
