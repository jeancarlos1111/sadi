<div class="row">
    <div class="col-md-8 offset-md-2">
        <div class="card card-outline card-primary">
            <div class="card-header">
                <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
            </div>
            <form method="POST" action="?route=presupuesto/guardar">
                <div class="card-body">
                    <?php if (isset($error)): ?>
                        <div class="alert alert-danger alert-dismissible">
                            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                            <h5><i class="icon fas fa-ban"></i> Error</h5>
                            <?= htmlspecialchars($error) ?>
                        </div>
                    <?php endif; ?>

                    <?php if ($partida && $partida->id): ?>
                        <input type="hidden" name="id" value="<?= $partida->id ?>">
                    <?php endif; ?>

                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label font-weight-bold">Estructura Presupuestaria:</label>
                        <div class="col-sm-8">
                            <select name="id_estruc_presupuestaria" class="form-control select2" required>
                                <option value="">-- Seleccione --</option>
                                <?php foreach ($estructuras as $e): ?>
                                    <option value="<?= $e['id_estruc_presupuestaria'] ?>"
                                        <?= ($partida && $partida->idEstructura == $e['id_estruc_presupuestaria']) ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($e['descripcion_ep']) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label font-weight-bold">Partida Presupuestaria:</label>
                        <div class="col-sm-8">
                            <select name="id_codigo_plan_unico" class="form-control select2" required>
                                <option value="">-- Seleccione --</option>
                                <?php foreach ($planUnico as $p): ?>
                                    <option value="<?= $p['id_codigo_plan_unico'] ?>"
                                        <?= ($partida && $partida->idPlanUnico == $p['id_codigo_plan_unico']) ? 'selected' : '' ?>>
                                        <?= htmlspecialchars($p['codigo_plan_unico']) ?> — <?= htmlspecialchars($p['denominacion']) ?>
                                    </option>
                                <?php endforeach; ?>
                            </select>
                        </div>
                    </div>

                    <div class="form-group row">
                        <label class="col-sm-4 col-form-label font-weight-bold">Monto Asignado (Bs):</label>
                        <div class="col-sm-8">
                            <input type="number" class="form-control form-control-lg text-primary font-weight-bold" name="monto_asignado" step="0.01" min="0.01" required
                                value="<?= $partida ? number_format($partida->montoAsignado, 2, '.', '') : '' ?>">
                            <small class="form-text text-muted">Solo se puede modificar el Monto Asignado original. Comprometidos y Causados se calculan automáticamente.</small>
                        </div>
                    </div>

                    <?php if ($partida && $partida->id): ?>
                    <div class="alert alert-secondary mt-4">
                        <h5><i class="icon fas fa-chart-pie"></i> Estado de Ejecución Actual</h5>
                        <ul class="list-unstyled mb-0">
                            <li><strong>Comprometido:</strong> Bs <?= number_format($partida->montoComprometido, 2, ',', '.') ?></li>
                            <li><strong>Causado:</strong> Bs <?= number_format($partida->montoCausado, 2, ',', '.') ?></li>
                            <li><strong>Pagado:</strong> Bs <?= number_format($partida->montoPagado, 2, ',', '.') ?></li>
                        </ul>
                    </div>
                    <?php endif; ?>
                </div>

                <div class="card-footer text-right">
                    <a href="?route=presupuesto/formulacion" class="btn btn-default mr-2">Cancelar</a>
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Guardar Partida
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
