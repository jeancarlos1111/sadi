<?php if (isset($error)): ?>
  <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
<?php endif; ?>

<div class="card card-primary card-outline">
  <?= render_component('page_header', [
        'title' => $titulo,
        'icon' => 'fas fa-coins',
        'newUrl' => '?route=fuente_financiamiento/form',
        'newText' => 'Nueva Fuente'
      ]) ?>
  <div class="card-body">
    <?= render_component('search_filter', [
        'route' => 'fuente_financiamiento/index',
        'searchValue' => $search ?? ''
      ]) ?>

    <table class="table table-bordered table-striped w-100">
      <thead class="thead-dark">
        <tr>
          <th>ID</th>
          <th>Denominación</th>
          <th class="text-right">Acciones</th>
        </tr>
      </thead>
      <tbody>
        <?php if (empty($items)): ?>
          <tr>
            <td colspan="3" class="text-center text-muted py-4">No se encontraron fuentes de financiamiento.</td>
          </tr>
        <?php else: ?>
          <?php foreach ($items as $item): ?>
            <tr>
              <td class="align-middle"><?= htmlspecialchars((string)$item->id) ?></td>
              <td class="align-middle"><?= htmlspecialchars($item->denominacion) ?></td>
              <td class="text-right align-middle">
                <?= render_component('table_actions', [
                'editUrl' => '?route=fuente_financiamiento/form&id=' . $item->id,
                'deleteUrl' => '?route=fuente_financiamiento/delete',
                'deleteId' => $item->id
            ]) ?>
              </td>
            </tr>
          <?php endforeach; ?>
        <?php endif; ?>
      </tbody>
    </table>
    <?php if (isset($paginator) && $paginator !== null): ?>
      <?php include $partialsPath . '/pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
