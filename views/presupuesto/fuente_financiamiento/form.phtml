<div class="row mb-4 align-items-center">
    <div class="col">
        <h2 class="mb-0 text-primary">
            <i class="fas <?= $item ? 'fa-edit' : 'fa-plus' ?> me-2"></i> 
            <?= htmlspecialchars($titulo) ?>
        </h2>
    </div>
    <div class="col-auto">
        <a href="?route=fuente_financiamiento/index" class="btn btn-secondary shadow-sm">
            <i class="fas fa-arrow-left me-1"></i> Volver
        </a>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form method="POST" action="?route=fuente_financiamiento/save">
            <?php if ($item && $item->id): ?>
                <input type="hidden" name="id" value="<?= htmlspecialchars((string)$item->id) ?>">
            <?php endif; ?>
            
            <div class="mb-3">
                <label for="denominacion" class="form-label">Denominación</label>
                <input type="text" class="form-control" id="denominacion" name="denominacion" 
                       value="<?= htmlspecialchars($item ? $item->denominacion : '') ?>" required>
            </div>
            
            <div class="text-end">
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-save me-1"></i> Guardar
                </button>
            </div>
        </form>
    </div>
</div>
