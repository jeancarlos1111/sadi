<div class="card card-primary card-outline">
  <?= render_component('page_header', [
        'title' => $titulo,
        'icon' => 'fas fa-calendar-alt',
        'newUrl' => '?route=p_a_c/form',
        'newText' => 'Agregar al PAC'
      ]) ?>
  <div class="card-body">
    <?= render_component('search_filter', [
        'route' => 'p_a_c/index',
        'searchValue' => $search ?? '',
        'placeholder' => 'Buscar artículo o proyecto...'
      ]) ?>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100">
        <thead class="thead-dark">
          <tr>
            <th>Artículo</th>
            <th>Proyecto/AC</th>
            <th class="text-right">Cant. Anual</th>
            <th class="text-right">Trim 1</th>
            <th class="text-right">Trim 2</th>
            <th class="text-right">Trim 3</th>
            <th class="text-right">Trim 4</th>
            <th class="text-right">Costo Est. (Bs)</th>
            <th class="text-center">Estatus</th>
            <th class="text-right">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($items)): ?>
            <tr>
              <td colspan="10" class="text-center text-muted py-4">No se encontraron renglones en el PAC.</td>
            </tr>
          <?php else: ?>
            <?php foreach ($items as $item): ?>
              <tr>
                <td class="align-middle"><?= htmlspecialchars($item['articulo_desc'] ?? '') ?></td>
                <td class="align-middle"><?= htmlspecialchars($item['proyecto_desc'] ?: $item['ac_desc']) ?></td>
                <td class="text-right align-middle"><?= number_format($item['entity']->cantidad_anual, 2, ',', '.') ?></td>
                <td class="text-right align-middle"><?= number_format($item['entity']->trim_1, 2, ',', '.') ?></td>
                <td class="text-right align-middle"><?= number_format($item['entity']->trim_2, 2, ',', '.') ?></td>
                <td class="text-right align-middle"><?= number_format($item['entity']->trim_3, 2, ',', '.') ?></td>
                <td class="text-right align-middle"><?= number_format($item['entity']->trim_4, 2, ',', '.') ?></td>
                <td class="text-right align-middle text-primary font-weight-bold"><?= number_format($item['entity']->costo_estimado, 2, ',', '.') ?></td>
                <td class="text-center align-middle">
                  <?php if($item['entity']->estatus === 'APROBADO'): ?>
                      <span class="badge badge-success">Aprobado</span>
                  <?php else: ?>
                      <span class="badge badge-warning">Planificado</span>
                  <?php endif; ?>
                </td>
                <td class="text-right align-middle text-nowrap">
                  <?php if($item['entity']->estatus !== 'APROBADO'): ?>
                  <form method="POST" action="?route=p_a_c/aprobar" style="display: inline-block;" onsubmit="return confirm('¿Aprobar y pre-comprometer este renglón?');">
                    <input type="hidden" name="id" value="<?= $item['entity']->id ?>">
                    <button type="submit" class="btn btn-sm btn-success" title="Aprobar">
                      <i class="fas fa-check"></i>
                    </button>
                  </form>
                  <?php endif; ?>
                  <?= render_component('table_actions', [
                'editUrl' => '?route=p_a_c/form&id=' . $item['entity'],
                'deleteUrl' => '?route=p_a_c/delete',
                'deleteId' => $item['entity']
            ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
        </tbody>
      </table>
    </div>
    <?php if (isset($paginator) && $paginator !== null): ?>
      <?php include $partialsPath . '/pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
