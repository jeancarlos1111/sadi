<div class="row mb-4 align-items-center">
    <div class="col">
        <h2 class="mb-0 text-primary">
            <i class="fas <?= $item ? 'fa-edit' : 'fa-plus' ?> me-2"></i> 
            <?= htmlspecialchars($titulo) ?>
        </h2>
    </div>
    <div class="col-auto">
        <a href="?route=p_a_c/index" class="btn btn-secondary shadow-sm">
            <i class="fas fa-arrow-left me-1"></i> Volver
        </a>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form method="POST" action="?route=p_a_c/save">
            <?php if ($item && $item->id): ?>
                <input type="hidden" name="id" value="<?= htmlspecialchars((string)$item->id) ?>">
            <?php endif; ?>
            
            <div class="row">
                <div class="col-md-12 mb-3">
                    <label class="form-label">Artículo</label>
                    <select class="form-select" name="id_articulo" required>
                        <option value="">Seleccione...</option>
                        <?php foreach ($articulos as $a): ?>
                            <option value="<?= $a['entity']->id ?>" <?= ($item && $item->id_articulo == $a['entity']->id) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($a['entity']->denominacion) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                
                <div class="col-md-6 mb-3">
                    <label class="form-label">Proyecto (Opcional si es AC)</label>
                    <select class="form-select" name="id_proyecto">
                        <option value="">Ninguno</option>
                        <?php foreach ($proyectos as $p): ?>
                            <option value="<?= htmlspecialchars((string)$p->id) ?>" <?= ($item && $item->id_proyecto == $p->id) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($p->denominacion) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
                
                <div class="col-md-6 mb-3">
                    <label class="form-label">Acción Centralizada (Opcional si es Proy.)</label>
                    <select class="form-select" name="id_accion_centralizada">
                        <option value="">Ninguno</option>
                        <?php foreach ($acciones as $ac): ?>
                            <option value="<?= htmlspecialchars((string)$ac->id) ?>" <?= ($item && $item->id_accion_centralizada == $ac->id) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($ac->denominacion) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </div>
            </div>

            <div class="row mt-3">
                <div class="col-md-12"><h5 class="text-primary border-bottom pb-2">Programación Física</h5></div>
                <div class="col-md-4 mb-3">
                    <label class="form-label">Cantidad Anual</label>
                    <input type="number" step="0.01" class="form-control" name="cantidad_anual" value="<?= $item ? $item->cantidad_anual : 0 ?>">
                </div>
                <div class="col-md-4 mb-3">
                    <label class="form-label">Costo Estimado Unitario (Bs)</label>
                    <input type="number" step="0.01" class="form-control text-primary font-weight-bold" name="costo_estimado" value="<?= $item ? $item->costo_estimado : 0 ?>">
                </div>
            </div>
            
            <div class="row">
                <div class="col-md-3 mb-3">
                    <label class="form-label">Trimestre 1</label>
                    <input type="number" step="0.01" class="form-control" name="trim_1" value="<?= $item ? $item->trim_1 : 0 ?>">
                </div>
                <div class="col-md-3 mb-3">
                    <label class="form-label">Trimestre 2</label>
                    <input type="number" step="0.01" class="form-control" name="trim_2" value="<?= $item ? $item->trim_2 : 0 ?>">
                </div>
                <div class="col-md-3 mb-3">
                    <label class="form-label">Trimestre 3</label>
                    <input type="number" step="0.01" class="form-control" name="trim_3" value="<?= $item ? $item->trim_3 : 0 ?>">
                </div>
                <div class="col-md-3 mb-3">
                    <label class="form-label">Trimestre 4</label>
                    <input type="number" step="0.01" class="form-control" name="trim_4" value="<?= $item ? $item->trim_4 : 0 ?>">
                </div>
            </div>
            
            <div class="text-end mt-4">
                <button type="submit" class="btn btn-success">
                    <i class="fas fa-save me-1"></i> Guardar Programación
                </button>
            </div>
        </form>
    </div>
</div>
