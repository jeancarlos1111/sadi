<?php
/**
 * @var array $trabajadores
 * @var string|null $error
 */
?>
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Pasivo Laboral (Prestaciones Sociales)</h2>
    <a href="?route=prestaciones/procesarTrimestre" class="btn btn-primary">
        <i class="bi bi-gear-fill me-1"></i> Procesar Trimestre
    </a>
</div>

<?php if (isset($_GET['success'])): ?>
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <?= htmlspecialchars($_GET['success']) ?>
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
<?php endif; ?>

<?php if (isset($error)): ?>
    <div class="alert alert-danger">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div class="card shadow-sm">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>Cédula</th>
                        <th>Nombres y Apellidos</th>
                        <th>Fecha de Ingreso</th>
                        <th>Sueldo Mensual Base</th>
                        <th>Días Acumulados</th>
                        <th class="text-end">Fondo Acumulado (Bs)</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($trabajadores)): ?>
                        <tr>
                            <td colspan="7" class="text-center py-4">No hay trabajadores activos o no se han procesado prestaciones.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($trabajadores as $t): ?>
                            <tr>
                                <td><?= htmlspecialchars($t['cedula']) ?></td>
                                <td><?= htmlspecialchars($t['nombres'] . ' ' . $t['apellidos']) ?></td>
                                <td><?= htmlspecialchars($t['ingreso']) ?></td>
                                <td>Bs. <?= number_format((float)$t['sueldo_basico'], 2, ',', '.') ?></td>
                                <td><?= (int)$t['dias_acumulados'] ?> días</td>
                                <td class="text-end fw-bold">
                                    Bs. <?= number_format((float)$t['total_acumulado'], 2, ',', '.') ?>
                                </td>
                                <td class="text-center">
                                    <a href="?route=prestaciones/estadoCuenta&cod_ficha=<?= $t['cod_ficha'] ?>" class="btn btn-sm btn-outline-info" title="Estado de Cuenta">
                                        <i class="bi bi-journal-text"></i> Histórico
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
    </div>
    <div class="card-footer clearfix">
        <?= render_component('pagination', ['page' => $page, 'totalPages' => $totalPages]) ?>
    </div>
</div>
