<?php
/**
 * @var array $trabajador
 * @var \App\Models\PrestacionGarantia[] $movimientos
 */
$totalAcumulado = 0.0;
$diasAcumulados = 0;
foreach ($movimientos as $m) {
    $totalAcumulado += $m->monto;
    $diasAcumulados += $m->diasDepositados;
}
?>
<div class="d-flex justify-content-between align-items-center mb-4">
    <h2>Estado de Cuenta: Prestaciones Sociales</h2>
    <a href="?route=prestaciones/index" class="btn btn-outline-secondary">
        <i class="bi bi-arrow-left"></i> Volver
    </a>
</div>

<div class="row mb-4">
    <div class="col-md-8">
        <div class="card shadow-sm h-100">
            <div class="card-header bg-white fw-bold">
                Datos del Trabajador
            </div>
            <div class="card-body">
                <div class="row">
                    <div class="col-sm-6 mb-2">
                        <span class="text-muted d-block small">Nombres y Apellidos</span>
                        <strong><?= htmlspecialchars($trabajador['nombres'] . ' ' . $trabajador['apellidos']) ?></strong>
                    </div>
                    <div class="col-sm-6 mb-2">
                        <span class="text-muted d-block small">Cédula de Identidad</span>
                        <strong><?= htmlspecialchars($trabajador['cedula']) ?></strong>
                    </div>
                    <div class="col-sm-6 mb-2">
                        <span class="text-muted d-block small">Fecha de Ingreso</span>
                        <strong><?= htmlspecialchars($trabajador['ingreso']) ?></strong>
                    </div>
                    <div class="col-sm-6 mb-2">
                        <span class="text-muted d-block small">Sueldo Básico Actual</span>
                        <strong>Bs. <?= number_format((float)$trabajador['sueldo_basico'], 2, ',', '.') ?></strong>
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card shadow-sm bg-primary text-white h-100">
            <div class="card-body d-flex flex-column justify-content-center text-center">
                <h5 class="card-title opacity-75">Fondo de Garantía Acumulado</h5>
                <h2 class="display-6 fw-bold mb-0">Bs. <?= number_format($totalAcumulado, 2, ',', '.') ?></h2>
                <div class="mt-2 opacity-75">
                    <i class="bi bi-clock-history"></i> <?= $diasAcumulados ?> Días Acreditados
                </div>
            </div>
        </div>
    </div>
</div>

<div class="card shadow-sm">
    <div class="card-header bg-white fw-bold">
        Histórico de Depósitos de Garantía (Art. 142 LOTTT)
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-hover table-striped align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th class="ps-3">ID</th>
                        <th>Fecha de Proceso</th>
                        <th>Período</th>
                        <th>Tipo</th>
                        <th class="text-center">Días</th>
                        <th class="text-end">Salario Int. Diario</th>
                        <th class="text-end pe-3">Monto Acreditado</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($movimientos)): ?>
                        <tr>
                            <td colspan="7" class="text-center py-4 text-muted">No existen depósitos registrados para este trabajador.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($movimientos as $mov): ?>
                            <tr>
                                <td class="ps-3 text-muted">#<?= $mov->id ?></td>
                                <td><?= date('d/m/Y', strtotime($mov->fechaProceso)) ?></td>
                                <td><span class="badge bg-info text-dark"><?= htmlspecialchars($mov->periodo) ?></span></td>
                                <td><?= htmlspecialchars($mov->tipo) ?></td>
                                <td class="text-center fw-bold"><?= $mov->diasDepositados ?></td>
                                <td class="text-end">Bs. <?= number_format($mov->salarioIntegralDiario, 2, ',', '.') ?></td>
                                <td class="text-end pe-3 fw-bold text-success">
                                    + Bs. <?= number_format($mov->monto, 2, ',', '.') ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
