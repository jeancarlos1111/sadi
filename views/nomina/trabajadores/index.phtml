<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=nomina/conceptos" class="btn btn-sm btn-info">
                <i class="fas fa-calculator"></i> Conceptos y Fórmulas
            </a>
            <a href="?route=nomina/planillas" class="btn btn-sm btn-success ml-1">
                <i class="fas fa-file-invoice-dollar"></i> Planillas de Nómina
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <form method="GET" action="" class="form-inline mb-4">
            <input type="hidden" name="route" value="nomina/trabajadores">
            <div class="input-group mr-2">
                <input type="text" class="form-control" name="search" placeholder="Cédula, Nombres..." value="<?= htmlspecialchars($search) ?>" style="width: 300px;">
                <span class="input-group-append">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
                </span>
            </div>
            <a href="?route=nomina/trabajadores" class="btn btn-default">Limpiar</a>
        </form>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Cédula</th>
                        <th>Nombres y Apellidos</th>
                        <th>Cargo Asignado</th>
                        <th>Nómina a la que Pertenece</th>
                        <th class="text-right">Sueldo Básico</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($trabajadores)): ?>
                        <tr><td colspan="5" class="text-center">No hay trabajadores registrados en la base de datos.</td></tr>
                    <?php else: ?>
                        <?php foreach ($trabajadores as $data): ?>
                            <?php $per = $data['entity']; ?>
                            <tr>
                                <td class="font-weight-bold"><?= htmlspecialchars($per->cedula) ?></td>
                                <td><?= htmlspecialchars($per->apellidos . ', ' . $per->nombres) ?></td>
                                <td><?= htmlspecialchars($data['cargo']) ?></td>
                                <td><?= htmlspecialchars($data['nomina']) ?></td>
                                <td class="text-right text-success font-weight-bold">
                                    <?= number_format($data['sueldo'], 2, ',', '.') ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
