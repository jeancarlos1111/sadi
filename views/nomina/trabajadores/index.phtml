<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="nomina/trabajadores">
        <input type="text" name="search" placeholder="Cédula, Nombres..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 300px;">
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=nomina/trabajadores" style="margin-left: 10px;">Limpiar</a>
    </form>
    <div>
        <a href="?route=nomina/conceptos" style="background-color: #17a2b8; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px; margin-right: 10px;">Conceptos y Fórmulas</a>
        <a href="?route=nomina/planillas" style="background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">Planillas de Nómina</a>
    </div>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>Cédula</th>
            <th>Nombres y Apellidos</th>
            <th>Cargo Asignado</th>
            <th>Nómina a la que Pertenece</th>
            <th>Sueldo Básico</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($trabajadores)): ?>
            <tr>
                <td colspan="5" style="text-align: center;">No hay trabajadores registrados en la base de datos.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($trabajadores as $data): ?>
                <?php $per = $data['entity']; ?>
                <tr>
                    <td style="font-weight: bold;"><?= htmlspecialchars($per->cedula) ?></td>
                    <td><?= htmlspecialchars($per->apellidos . ', ' . $per->nombres) ?></td>
                    <td><?= htmlspecialchars($data['cargo']) ?></td>
                    <td><?= htmlspecialchars($data['nomina']) ?></td>
                    <td style="text-align: right; color: green;">
                        <?= number_format($data['sueldo'], 2, ',', '.') ?>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
