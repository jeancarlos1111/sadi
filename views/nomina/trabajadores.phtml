<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=nomina/crearTrabajador" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Registrar Nuevo Trabajador
      </a>
    </div>
  </div>
  <div class="card-body">
    <div class="callout callout-info mb-4">
      <h5>Gestión de Personal</h5>
      <p>Este es el listado de trabajadores activos registrados en la base de datos de SADI. Estos datos alimentan automáticamente el Motor de Nómina.</p>
      <div class="alert alert-info mb-0">
        <h5><i class="icon fas fa-info"></i> Nota del MVP</h5>
        Actualmente se muestran los registros insertados desde la inicialización del sistema (Seed Data). La pantalla de "Registrar Nuevo Trabajador", "Cambio de Cargo" e "Inclusión de Familiares" pertenece al desarrollo del Módulo Completo de RRHH que excede el ciclo de transacciones financieras de esta migración.
      </div>
    </div>

    <?php if (isset($_GET['success'])): ?>
      <div class="alert alert-success alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <h5><i class="icon fas fa-check"></i> Éxito</h5>
        <?= htmlspecialchars($_GET['success']) ?>
      </div>
    <?php endif; ?>

    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100 table-sm">
        <thead class="bg-info text-white">
          <tr>
            <th>Cédula</th>
            <th>Nombres y Apellidos</th>
            <th>Cargo</th>
            <th>Tipo de Nómina</th>
            <th>Fecha de Ingreso</th>
            <th class="text-right">Sueldo Base Asignado</th>
            <th class="text-center">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($trabajadores)): ?>
            <tr><td colspan="7" class="text-center">No hay trabajadores activos en el sistema.</td></tr>
          <?php else: ?>
            <?php foreach ($trabajadores as $t): ?>
              <tr>
                <td class="font-weight-bold"><?= htmlspecialchars($t['cedula']) ?></td>
                <td>
                  <?= htmlspecialchars($t['nombres'] . ' ' . $t['apellidos']) ?>
                  <?php if (empty($t['banco'])): ?>
                    <span class="badge badge-warning ml-1" title="Falta cuenta bancaria"><i class="fas fa-exclamation-triangle"></i> Sin Banco</span>
                  <?php endif; ?>
                </td>
                <td><?= htmlspecialchars($t['cargo']) ?></td>
                <td><span class="badge badge-secondary" style="font-size: 0.9em;"><?= htmlspecialchars($t['nomina']) ?></span></td>
                <td><?= htmlspecialchars($t['ingreso']) ?></td>
                <td class="text-right font-weight-bold text-success">Bs <?= number_format((float)$t['sueldo_basico'], 2, ',', '.') ?></td>
                <td class="text-center">
                  <?= render_component('table_actions', [
                      'editUrl' => '?route=nomina/editarTrabajador&cod_ficha=' . htmlspecialchars((string)$t['cod_ficha'])
                  ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
      </table>
    </div>
    <div class="card-footer clearfix mt-3">
      <?= render_component('pagination', ['page' => $page, 'totalPages' => $totalPages]) ?>
    </div>
  </div>
</div>
