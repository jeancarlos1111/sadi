<h2><?= htmlspecialchars($titulo) ?></h2>

<div style="background-color: #f8f9fa; padding: 20px; border: 1px solid #ddd; border-radius: 4px; margin-bottom: 20px;">
    <p><strong>Trabajador:</strong> <?= htmlspecialchars($trabajador['nombres'] . ' ' . $trabajador['apellidos']) ?> (C.I: <?= htmlspecialchars($trabajador['cedula']) ?>)</p>
    <p style="margin: 0; color: #666; font-size: 0.9em;">Actualice el cargo, nómina o sueldo base. Estos cambios tendrán efecto inmediato en la próxima generación de planilla.</p>
</div>

<form method="POST" action="?route=nomina/actualizarTrabajador" style="max-width: 600px; border: 1px solid #ced4da; padding: 20px; border-radius: 8px; background: white;">
    
    <input type="hidden" name="cod_ficha" value="<?= htmlspecialchars($trabajador['cod_ficha']) ?>">

    <div style="margin-bottom: 15px;">
        <label style="display: block; font-weight: bold; margin-bottom: 5px;">Cargo Actual:</label>
        <select name="cod_cargo" required style="width: 100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px;">
            <?php foreach ($cargos as $c): ?>
                <option value="<?= $c->id ?>" <?= $c->id == $trabajador['cargo_cod_cargo'] ? 'selected' : '' ?>>
                    <?= htmlspecialchars($c->nombre) ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>

    <div style="margin-bottom: 15px;">
        <label style="display: block; font-weight: bold; margin-bottom: 5px;">Pertenece a Nómina:</label>
        <select name="cod_nomina" required style="width: 100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px;">
            <?php foreach ($nominas as $n): ?>
                <option value="<?= $n->id ?>" <?= $n->id == $trabajador['nomina_cod_nomina'] ? 'selected' : '' ?>>
                    <?= htmlspecialchars($n->nombre) ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>

    <div style="margin-bottom: 20px;">
        <label style="display: block; font-weight: bold; margin-bottom: 5px;">Sueldo Básico Modificado (Bs):</label>
        <input type="number" step="0.01" min="1" name="sueldo_basico" required value="<?= htmlspecialchars($trabajador['sueldo_basico']) ?>" style="width: 100%; padding: 8px; border: 1px solid #ccc; border-radius: 4px; font-weight: bold; color: green;">
    </div>

    <div style="text-align: right; margin-top: 30px; border-top: 1px solid #eee; padding-top: 15px;">
        <a href="?route=nomina/trabajadores" style="margin-right: 15px; text-decoration: none; color: #555;">Cancelar</a>
        <button type="submit" style="background-color: #ffc107; color: #000; padding: 10px 20px; border: none; border-radius: 4px; font-weight: bold; cursor: pointer;">
            Guardar Cambios
        </button>
    </div>
</form>
