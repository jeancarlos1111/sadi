<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=tipos_articulos/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nuevo Tipo
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <table class="table table-bordered table-striped " style="width:100%;">
      <thead>
        <tr>
          <th>Denominación</th>
          <th>Descripción</th>
          <th>Tipo</th>
          <th width="15%">Acciones</th>
        </tr>
      </thead>
      <tbody>
    <?php if (empty($items)): ?>
      <tr><td colspan="4" style="text-align:center;">No hay tipos registrados.</td></tr>
    <?php else: foreach ($items as $item): ?>
      <tr>
        <td><?= htmlspecialchars($item->denominacion) ?></td>
        <td><?= htmlspecialchars($item->descripcion ?? '') ?></td>
        <td><?= $item->tipo === 1 ? 'Bien' : 'Insumo' ?></td>
        <td class="text-center">
          <?= render_component('table_actions', [
                'editUrl' => '?route=tipos_articulos/form&id=' . $item->id,
                'deleteUrl' => '?route=tipos_articulos/eliminar',
                'deleteId' => $item->id
            ]) ?>
        </td>
      </tr>
      <?php endforeach; endif; ?>
    </tbody>
    </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
