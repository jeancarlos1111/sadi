<?php
/** @var array $actas */
/** @var string $search */
?>
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Actas de Recepción</h5>
        <a href="?route=inventario_recepcion/form" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-lg"></i> Nueva Acta
        </a>
    </div>
    <div class="card-body">
        <div class="table-responsive ajax-table-container">
            <table class="table table-hover table-sm">
                <thead>
                    <tr>
                        <th>Número de Acta</th>
                        <th>Fecha</th>
                        <th>Orden de Compra ID</th>
                        <th>Conformidad</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if(empty($actas)): ?>
                        <tr><td colspan="5" class="text-center">No hay actas registradas</td></tr>
                    <?php else: ?>
                        <?php foreach($actas as $acta): ?>
                            <tr>
                                <td><?= htmlspecialchars($acta->numeroActa) ?></td>
                                <td><?= htmlspecialchars($acta->fechaRecepcion) ?></td>
                                <td><?= htmlspecialchars((string)$acta->idOrdenDeCompra) ?></td>
                                <td>
                                    <?php if ($acta->conformidad): ?>
                                        <span class="badge bg-success">Conforme</span>
                                    <?php else: ?>
                                        <span class="badge bg-danger">No Conforme</span>
                                    <?php endif; ?>
                                </td>
                                <td>
                                    <?= render_component('table_actions', [
                                        'editUrl' => '', 
                                        'deleteUrl' => '', 
                                        'deleteId' => $acta->id
                                    ]) ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
