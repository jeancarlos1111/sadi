<?php
/** @var string $titulo */
/** @var array $insumos */
/** @var string $search */
/** @var string|null $error */
?>
<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=inventario/entradas" class="btn btn-sm btn-success mr-1">
                <i class="fas fa-truck-loading"></i> Entradas (+ Recepción)
            </a>
            <a href="?route=inventario/despachos" class="btn btn-sm btn-warning">
                <i class="fas fa-dolly"></i> Salidas / Despachos
            </a>
        </div>
    </div>
    
    <div class="card-body">
        <?php if ($error): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <form method="GET" action="" class="form-inline mb-3">
            <input type="hidden" name="route" value="inventario/index">
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Buscar Artículo o Almacén..." value="<?= htmlspecialchars($search) ?>">
                <span class="input-group-append">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
                    <a href="?route=inventario/index" class="btn btn-default">Limpiar</a>
                </span>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100">
                <thead class="thead-dark">
                    <tr>
                        <th>Artículo / Insumo</th>
                        <th>Unidad de Medida</th>
                        <th class="text-right">Stock (Existencia Actual)</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($insumos)): ?>
                        <tr>
                            <td colspan="4" class="text-center">No hay artículos en existencia.</td>
                        </tr>
                    <?php else: ?>
                        <?php foreach ($insumos as $data): ?>
                            <?php $item = $data['entity']; ?>
                            <tr>
                                <td><strong><?= htmlspecialchars($data['articulo_desc']) ?></strong></td>
                                <td><?= htmlspecialchars($data['unidad']) ?></td>
                                <td class="text-right font-weight-bold <?= $item->cantidad > 0 ? 'text-success' : 'text-danger' ?>">
                                    <?= number_format($item->cantidad, 2, ',', '.') ?>
                                </td>
                                <td class="text-center">
                                    <a href="#" class="btn btn-sm btn-info" title="Ver Kardex">
                                        <i class="fas fa-clipboard-list"></i> Kardex
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
    <?php if (isset($paginator)): ?>
        <div class="card-footer clearfix">
            <?php include $partialsPath . 'pagination.phtml'; ?>
        </div>
    <?php endif; ?>
</div>
