<?php
/** @var array $asignaciones */
?>
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Asignaciones de Bienes Nacionales</h5>
        <a href="?route=asignacion_bienes/form" class="btn btn-primary btn-sm">
            <i class="bi bi-plus-lg"></i> Nueva Asignación
        </a>
    </div>
    <div class="card-body">
        <div class="table-responsive ajax-table-container">
            <table class="table table-hover table-sm">
                <thead>
                    <tr>
                        <th>N° Asignación</th>
                        <th>Fecha</th>
                        <th>Articulo ID</th>
                        <th>Cédula Responsable</th>
                        <th>Estado</th>
                        <th>Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if(empty($asignaciones)): ?>
                        <tr><td colspan="6" class="text-center">No hay asignaciones registradas</td></tr>
                    <?php else: ?>
                        <?php foreach($asignaciones as $asig): ?>
                            <tr>
                                <td><?= htmlspecialchars($asig->numeroAsignacion) ?></td>
                                <td><?= htmlspecialchars($asig->fechaAsignacion) ?></td>
                                <td><?= htmlspecialchars((string)$asig->idArticulo) ?></td>
                                <td><?= htmlspecialchars($asig->cedulaResponsable) ?></td>
                                <td>
                                    <?php if ($asig->estadoAsignacion === 'ACTIVA'): ?>
                                        <span class="badge bg-primary">Activa</span>
                                    <?php else: ?>
                                        <span class="badge bg-secondary"><?= htmlspecialchars($asig->estadoAsignacion) ?></span>
                                    <?php endif; ?>
                                </td>
                                <td>
                                    <?= render_component('table_actions', [
                                        'editUrl' => '', 
                                        'deleteUrl' => '', 
                                        'deleteId' => $asig->id
                                    ]) ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
