<div class="card card-outline card-success">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
  </div>
  <div class="card-body">
    <div class="alert alert-warning">
        <h5><i class="icon fas fa-exclamation-triangle"></i> Módulo en Desarrollo</h5>
        La emisión de nuevos comprobantes a partir del pago de facturas se encuentra en fase de desarrollo. Sin embargo, la selección del tipo de retención configurable (SENIAT) ya se encuentra funcional para cuando se active el cálculo automático.
    </div>

    <form method="POST" action="#">
        <div class="row">
            <div class="col-md-6 form-group">
                <label>Factura Relacionada</label>
                <select class="form-control" disabled>
                    <option>Seleccione una factura pagada...</option>
                </select>
            </div>
            <div class="col-md-6 form-group">
                <label>Tipo de Retención (Aplicación Ley SENIAT)</label>
                <select name="id_tipo_retencion" class="form-control" required>
                    <option value="">Seleccione el tipo de retención...</option>
                    <?php foreach ($tipos as $t): ?>
                        <option value="<?= htmlspecialchars((string)$t->id) ?>" data-pct="<?= htmlspecialchars((string)$t->porcentaje) ?>">
                            <?= htmlspecialchars($t->codigo . ' - ' . $t->denominacion . ' (' . number_format($t->porcentaje, 2, ',', '.') . '%)') ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>
        </div>
        
        <div class="text-right">
            <a href="?route=retenciones/index" class="btn btn-secondary">Volver</a>
            <button type="button" class="btn btn-success" disabled>Emitir Comprobante</button>
        </div>
    </form>
  </div>
</div>
