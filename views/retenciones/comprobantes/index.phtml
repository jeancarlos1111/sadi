<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=retenciones/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Emitir Comprobante
            </a>
            <a href="?route=retenciones/declarar" class="btn btn-sm btn-info ml-1">
                <i class="fas fa-file-export"></i> Declaración Seniat
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <form method="GET" action="" class="form-inline mb-4">
            <input type="hidden" name="route" value="retenciones/index">
            <div class="input-group mr-2">
                <input type="text" class="form-control" name="search" placeholder="Nro de Comprobante, Factura o RIF..." value="<?= htmlspecialchars($search) ?>" style="width: 350px;">
                <span class="input-group-append">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
                    <a href="?route=retenciones/index" class="btn btn-default">Limpiar</a>
                </span>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Fecha</th>
                        <th>Tipo</th>
                        <th>Nº Comprobante</th>
                        <th>Factura Origen</th>
                        <th>Proveedor</th>
                        <th class="text-center">%</th>
                        <th class="text-right">Monto Retenido</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($comprobantes)): ?>
                        <tr><td colspan="8" class="text-center py-4">No hay comprobantes de retención.</td></tr>
                    <?php else: ?>
                        <?php foreach ($comprobantes as $data): ?>
                            <?php $comp = $data['entity']; ?>
                            <tr>
                                <td class="text-nowrap"><?= htmlspecialchars($comp->fechaEmision) ?></td>
                                <td class="font-weight-bold text-info"><?= htmlspecialchars($comp->tipoRetencion) ?></td>
                                <td class="font-weight-bold text-primary"><?= htmlspecialchars($comp->numeroComprobante) ?></td>
                                <td>
                                    <?= htmlspecialchars($data['factura_str']) ?><br>
                                    <small class="text-muted font-weight-bold">Mto: <?= number_format($data['factura_monto'], 2, ',', '.') ?></small>
                                </td>
                                <td><?= htmlspecialchars($data['proveedor']) ?></td>
                                <td class="text-center align-middle font-weight-bold">
                                    <span class="badge badge-secondary"><?= number_format($comp->porcentaje, 2, ',', '.') ?>%</span>
                                </td>
                                <td class="text-right font-weight-bold text-danger align-middle">
                                    <?= number_format($comp->montoRetenido, 2, ',', '.') ?>
                                </td>
                                <td class="text-center align-middle">
                                    <a href="?route=documental/comprobanteRetencion&id=<?= $comp->id ?>" target="_blank" class="btn btn-sm btn-outline-success font-weight-bold text-nowrap" title="Ver PDF">
                                        <i class="fas fa-file-pdf"></i> PDF
                                    </a>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
