<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=plan_unico_cuentas/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nueva Cuenta
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <table class="table table-bordered table-striped w-100">
      <thead class="thead-dark">
        <tr>
          <th width="15%">Código</th>
          <th>Denominación</th>
          <th width="15%">Acciones</th>
        </tr>
      </thead>
      <tbody>
    <?php if (empty($items)): ?>
      <tr><td colspan="3" class="text-center">No hay cuentas registradas.</td></tr>
    <?php else: foreach ($items as $item): ?>
      <tr>
        <td><strong><?= htmlspecialchars($item->codigo) ?></strong></td>
        <td><?= htmlspecialchars($item->denominacion) ?></td>
        <td class="text-center">
          <?= render_component('table_actions', [
                'editUrl' => '?route=plan_unico_cuentas/form&id=' . $item->id,
                'deleteUrl' => '?route=plan_unico_cuentas/eliminar',
                'deleteId' => $item->id
            ]) ?>
        </td>
      </tr>
    <?php endforeach; endif; ?>
      </tbody>
    </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
