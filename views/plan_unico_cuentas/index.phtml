<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=plan_unico_cuentas/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Nueva Cuenta
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <table class="table table-bordered table-striped dataTable w-100">
            <thead>
                <tr>
                    <th width="15%">Código</th>
                    <th>Denominación</th>
                    <th width="15%">Acciones</th>
                </tr>
            </thead>
            <tbody>
        <?php if (empty($items)): ?>
            <tr><td colspan="3" class="text-center">No hay cuentas registradas.</td></tr>
        <?php else: foreach ($items as $item): ?>
            <tr>
                <td><strong><?= htmlspecialchars($item->codigo) ?></strong></td>
                <td><?= htmlspecialchars($item->denominacion) ?></td>
                <td class="text-center">
                    <a href="?route=plan_unico_cuentas/form&id=<?= $item->id ?>" class="btn btn-sm btn-info" title="Editar">
                        <i class="fas fa-edit"></i>
                    </a>
                    <form method="POST" action="?route=plan_unico_cuentas/eliminar" onsubmit="return confirm('¿Eliminar esta cuenta?');" class="d-inline">
                        <input type="hidden" name="id" value="<?= $item->id ?>">
                        <button type="submit" class="btn btn-sm btn-danger" title="Eliminar">
                            <i class="fas fa-trash-alt"></i>
                        </button>
                    </form>
                </td>
            </tr>
        <?php endforeach; endif; ?>
            </tbody>
        </table>
    </div>
</div>
