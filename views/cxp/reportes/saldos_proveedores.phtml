<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <span class="text-muted small ml-3"><i class="fas fa-info-circle"></i> Información sincronizada con Presupuesto y Tesorería</span>
        <div class="card-tools">
            <a href="?route=reportes_cxp/saldosProveedores&pdf=1" target="_blank" class="btn btn-sm btn-danger">
                <i class="fas fa-file-pdf"></i> Exportar PDF
            </a>
            <a href="?route=cuentas_por_pagar/index" class="btn btn-sm btn-secondary ml-1">
                <i class="fas fa-arrow-left"></i> Volver a CxP
            </a>
        </div>
    </div>
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Referencia / RIF</th>
                        <th>Proveedor Beneficiario</th>
                        <th class="text-center">Volumen Facturas</th>
                        <th class="text-right">Base Imponible</th>
                        <th class="text-right">Impuestos (IVA)</th>
                        <th class="text-right">Deuda Total</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($saldos)): ?>
                        <tr><td colspan="6" class="text-center">No hay facturas pendientes de pago registradas por el momento. ¡Todo al día!</td></tr>
                    <?php else: ?>
                        <?php 
                        $grandTotal = 0;
                        foreach ($saldos as $s): 
                            $grandTotal += $s['total_deuda'];
                        ?>
                            <tr>
                                <td><strong><?= htmlspecialchars($s['rif_proveedor']) ?></strong></td>
                                <td><?= htmlspecialchars($s['compania_proveedor']) ?></td>
                                <td class="text-center">
                                    <span class="badge badge-info"><?= $s['cantidad_facturas'] ?></span>
                                </td>
                                <td class="text-right"><?= number_format($s['total_base'], 2, ',', '.') ?></td>
                                <td class="text-right"><?= number_format($s['total_iva'], 2, ',', '.') ?></td>
                                <td class="text-right font-weight-bold text-danger">
                                    <?= number_format($s['total_deuda'], 2, ',', '.') ?> Bs.
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
                <tfoot class="thead-light">
                    <tr>
                        <td colspan="5" class="text-right font-weight-bold pt-3 h6">TOTAL CARTERA PENDIENTE:</td>
                        <td class="text-right font-weight-bold text-danger h5 pt-2 mb-0">
                            <?= number_format($grandTotal ?? 0, 2, ',', '.') ?> Bs.
                        </td>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>
