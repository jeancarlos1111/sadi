<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($_GET['success'])): ?>
    <div style="color:green; padding:10px; border:1px solid green; margin-bottom:15px; border-radius:4px; background:#f0fff0;">
        ✔ <?= htmlspecialchars($_GET['success']) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom:20px; display:flex; justify-content:space-between; align-items:center;">
    <form method="GET" action="" style="margin:0;">
        <input type="hidden" name="route" value="deducciones_cxp/index">
        <input type="text" name="search" placeholder="Buscar deducción..." value="<?= htmlspecialchars($search) ?>" style="padding:5px; width:250px;">
        <button type="submit" style="padding:5px;">Buscar</button>
        <a href="?route=deducciones_cxp/index" style="margin-left:8px;">Limpiar</a>
    </form>
    <a href="?route=deducciones_cxp/form" style="background-color:#28a745; color:white; padding:10px 15px; text-decoration:none; border-radius:4px;">+ Nueva Deducción</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width:100%; border-collapse:collapse;">
    <thead style="background-color:#0056b3; color:white;">
        <tr>
            <th>Código</th>
            <th>Denominación</th>
            <th>Porcentaje (%)</th>
            <th>Aplica Sobre</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($items)): ?>
            <tr><td colspan="6" style="text-align:center;">No hay deducciones/retenciones registradas.</td></tr>
        <?php else: foreach ($items as $item): ?>
            <tr>
                <td><?= htmlspecialchars($item->codigo) ?></td>
                <td><?= htmlspecialchars($item->denominacion) ?></td>
                <td><?= number_format($item->porcentaje, 2, ',', '.') ?>%</td>
                <td><?= htmlspecialchars($item->aplicaSobre) ?></td>
                <td><?= $item->activo ? '<span style="color:green">Activo</span>' : '<span style="color:red">Inactivo</span>' ?></td>
                <td>
                    <a href="?route=deducciones_cxp/form&id=<?= $item->id ?>" style="margin-right:8px;">Editar</a>
                    <form method="POST" action="?route=deducciones_cxp/eliminar" onsubmit="return confirm('¿Seguro que desea eliminar esta deducción?');" style="display:inline;">
                        <input type="hidden" name="id" value="<?= $item->id ?>">
                        <button type="submit" style="color:red; border:none; background:none; cursor:pointer;">Eliminar</button>
                    </form>
                </td>
            </tr>
        <?php endforeach; endif; ?>
    </tbody>
</table>
