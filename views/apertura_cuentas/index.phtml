<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    </div>
    <div class="card-body">
        <?php if (isset($success)): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($success) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <div class="callout callout-info">
            <p>
                Seleccione la <strong>Estructura Presupuestaria</strong> para ver las cuentas del Plan Único que aún no han sido aperturadas.
                Coloque el monto inicial asignado a cada partida y haga clic en <strong>Generar Comprobante de Apertura</strong>.
            </p>
        </div>

        <form method="GET" action="" class="form-inline mb-4">
            <input type="hidden" name="route" value="apertura_cuentas/index">
            <label class="mr-2 font-weight-bold">Estructura Presupuestaria:</label>
            <select name="id_estruc_presupuestaria" class="form-control mr-2">
                <option value="">-- Seleccionar --</option>
                <?php foreach ($estructuras as $ep): ?>
                    <option value="<?= htmlspecialchars((string)$ep->id) ?>"
                        <?= ($id_estruc_seleccionada === $ep->id) ? 'selected' : '' ?>>
                        <?= htmlspecialchars($ep->descripcion) ?>
                    </option>
                <?php endforeach; ?>
            </select>
            <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Cargar cuentas por aperturar</button>
        </form>

        <?php if ($id_estruc_seleccionada && !empty($cuentas)): ?>

            <form method="POST" action="?route=apertura_cuentas/guardar">
                <input type="hidden" name="id_estruc_presupuestaria" value="<?= htmlspecialchars((string)$id_estruc_seleccionada) ?>">
                <input type="hidden" name="denominacion_c" value="APERTURA Y FORMULACIÓN INICIAL">

                <div class="row mb-3">
                    <div class="col-md-3">
                        <div class="form-group">
                            <label>Fecha del comprobante (*)</label>
                            <input type="date" class="form-control" name="fecha" value="<?= date('Y-m-d') ?>" required>
                        </div>
                    </div>
                    <div class="col-md-6">
                        <div class="form-group">
                            <label>Referencia / Denominación</label>
                            <input type="text" class="form-control" name="denominacion_c" value="APERTURA Y FORMULACIÓN INICIAL" maxlength="255">
                        </div>
                    </div>
                    <div class="col-md-3">
                        <div class="form-group">
                            <label>N° Referencia interna</label>
                            <input type="text" class="form-control" name="referencia_c" placeholder="Ej: OF-001/2025">
                        </div>
                    </div>
                </div>

                <table class="table table-bordered table-striped dataTable w-100">
                    <thead class="thead-dark">
                        <tr>
                            <th>Código</th>
                            <th>Denominación de la Partida</th>
                            <th style="width:180px;">Monto Inicial Asignado (Bs.)</th>
                        </tr>
                    </thead>
                    <tbody>
                <?php foreach ($cuentas as $cuenta): ?>
                    <tr>
                        <td><strong><?= htmlspecialchars($cuenta['codigo_plan_unico']) ?></strong></td>
                        <td><?= htmlspecialchars($cuenta['denominacion']) ?></td>
                        <td>
                            <input
                                type="number"
                                step="0.01"
                                min="0"
                                class="form-control text-right"
                                name="montos[<?= htmlspecialchars((string)$cuenta['id_codigo_plan_unico']) ?>]"
                                value="0"
                            >
                        </td>
                    </tr>
                <?php endforeach; ?>
                    </tbody>
                </table>

                <div class="mt-4">
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-file-invoice-dollar"></i> Generar Comprobante de Apertura
                    </button>
                    <a href="?route=apertura_cuentas/index" class="btn btn-default ml-2">Cancelar</a>
                </div>
            </form>

        <?php elseif ($id_estruc_seleccionada && empty($cuentas)): ?>
            <div class="alert alert-info">
                <h5><i class="icon fas fa-info"></i> Información</h5>
                Todas las cuentas de esta Estructura Presupuestaria ya han sido aperturadas.
            </div>
        <?php endif; ?>
    </div>
</div>
