<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=unidades_medida/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nueva Unidad
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <table class="table table-bordered table-striped w-100">
      <thead>
        <tr>
          <th>Denominación</th>
          <th>Sigla/Unidad</th>
          <th>Observación</th>
          <th width="15%">Acciones</th>
        </tr>
      </thead>
      <tbody>
    <?php if (empty($items)): ?>
      <tr><td colspan="4" style="text-align:center;">No hay unidades registradas.</td></tr>
    <?php else: foreach ($items as $item): ?>
      <tr>
        <td><?= htmlspecialchars($item->denominacion) ?></td>
        <td><?= htmlspecialchars($item->unidades) ?></td>
        <td><?= htmlspecialchars($item->observacion ?? '') ?></td>
        <td class="text-center">
          <?= render_component('table_actions', [
                'editUrl' => '?route=unidades_medida/form&id=' . $item->id,
                'deleteUrl' => '?route=unidades_medida/eliminar',
                'deleteId' => $item->id
            ]) ?>
        </td>
      </tr>
      <?php endforeach; endif; ?>
    </tbody>
    </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
