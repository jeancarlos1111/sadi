<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="administrador/usuarios">
        <input type="text" name="search" placeholder="Nombre de usuario o empleado..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 300px;">
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=administrador/usuarios" style="margin-left: 10px;">Limpiar</a>
    </form>
    <div>
        <a href="?route=administrador/configuracion" style="background-color: #ffc107; color: black; padding: 10px 15px; text-decoration: none; border-radius: 4px;">Ir a Configuración Global</a>
    </div>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>ID</th>
            <th>Nombre de Usuario</th>
            <th>Personal Asignado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($usuarios)): ?>
            <tr>
                <td colspan="4" style="text-align: center;">No hay usuarios registrados.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($usuarios as $data): ?>
                <?php $usr = $data['entity']; ?>
                <tr>
                    <td><?= $usr->id ?></td>
                    <td style="font-weight: bold;"><?= htmlspecialchars($usr->usuario) ?></td>
                    <td><?= htmlspecialchars($data['nombre_completo']) ?></td>
                    <td>
                        <a href="#">Editar</a> | 
                        <a href="#" style="color: red;">Restablecer Clave</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
