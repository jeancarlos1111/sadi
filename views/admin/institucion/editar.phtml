<?php 
/**
 * @var string $titulo
 * @var \App\Models\Institucion $config
 * @var string|null $error
 * @var bool $success
 */
?>
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2"><i class="fas fa-building mr-2 text-dark"></i> <?= htmlspecialchars($titulo) ?></h1>
</div>

<?php if (isset($error) && $error): ?>
    <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
<?php endif; ?>

<?php if (isset($success) && $success): ?>
    <div class="alert alert-success alert-dismissible fade show">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <i class="fas fa-check-circle mr-2"></i> Datos institucionales actualizados correctamente.
    </div>
<?php endif; ?>

<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title">Configuración Legal y de Contacto</h3>
    </div>
    
    <form action="?route=admin/institucionGuardar" method="POST" enctype="multipart/form-data">
        <div class="card-body">
            
            <div class="row">
                <!-- Columna Izquierda: Datos Básicos -->
                <div class="col-md-6">
                    <h5 class="text-primary border-bottom pb-2 mb-3">Datos Básicos y Legales</h5>
                    
                    <div class="form-group">
                        <label for="nombre">Nombre de la Institución <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="nombre" name="nombre" value="<?= htmlspecialchars($config->nombre) ?>" required>
                    </div>

                    <div class="form-group">
                        <label for="rif">R.I.F. <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="rif" name="rif" placeholder="Ej. G-20000000-0" value="<?= htmlspecialchars($config->rif) ?>" required>
                    </div>

                    <div class="form-group">
                        <label for="base_legal">Base Legal (Gaceta, Decreto de Creación)</label>
                        <input type="text" class="form-control" id="base_legal" name="base_legal" placeholder="Ej. Gaceta Oficial N° 40.000 de fecha..." value="<?= htmlspecialchars($config->base_legal ?? '') ?>">
                    </div>

                    <div class="form-group">
                        <label for="codigo_onapre">Código ONAPRE</label>
                        <input type="text" class="form-control" id="codigo_onapre" name="codigo_onapre" value="<?= htmlspecialchars($config->codigo_onapre ?? '') ?>">
                    </div>
                    
                    <div class="form-group">
                        <label for="direccion">Dirección Fiscal <span class="text-danger">*</span></label>
                        <textarea class="form-control" id="direccion" name="direccion" rows="3" required><?= htmlspecialchars($config->direccion) ?></textarea>
                    </div>
                </div>

                <!-- Columna Derecha: Contacto y Logo -->
                <div class="col-md-6">
                    <h5 class="text-primary border-bottom pb-2 mb-3">Contacto y Autoridades</h5>
                    
                    <div class="row">
                        <div class="col-md-6 form-group">
                            <label for="telefono">Teléfono</label>
                            <input type="text" class="form-control" id="telefono" name="telefono" value="<?= htmlspecialchars($config->telefono ?? '') ?>">
                        </div>
                        <div class="col-md-6 form-group">
                            <label for="correo">Correo Electrónico</label>
                            <input type="email" class="form-control" id="correo" name="correo" value="<?= htmlspecialchars($config->correo ?? '') ?>">
                        </div>
                    </div>

                    <div class="form-group">
                        <label for="maxima_autoridad">Nombre Máxima Autoridad (Cuentadante)</label>
                        <input type="text" class="form-control" id="maxima_autoridad" name="maxima_autoridad" placeholder="Ej. Juan Pérez" value="<?= htmlspecialchars($config->maxima_autoridad ?? '') ?>">
                    </div>

                    <div class="form-group">
                        <label for="cargo_autoridad">Cargo Máxima Autoridad</label>
                        <input type="text" class="form-control" id="cargo_autoridad" name="cargo_autoridad" placeholder="Ej. Alcalde, Presidente" value="<?= htmlspecialchars($config->cargo_autoridad ?? '') ?>">
                    </div>

                    <div class="form-group mt-4">
                        <label for="logo">Logo de la Institución</label>
                        <div class="custom-file">
                            <input type="file" class="custom-file-input" id="logo" name="logo" accept="image/png, image/jpeg">
                            <label class="custom-file-label" for="logo">Seleccionar archivo...</label>
                        </div>
                        <small class="form-text text-muted">Se recomienda formato PNG con fondo transparente (máximo 2MB).</small>
                        
                        <div class="mt-3 text-center">
                            <label class="d-block text-left">Vista Previa:</label>
                            <img id="logo-preview" src="<?= htmlspecialchars($config->logo_path ?? '') ?>" alt="Logo Institución" class="img-thumbnail" style="max-height: 150px; <?= !$config->logo_path ? 'display: none;' : '' ?>">
                        </div>
                    </div>
                </div>
            </div>

        </div>
        
        <div class="card-footer text-right bg-white">
            <a href="?route=admin/institucion" class="btn btn-default mr-2"><i class="fas fa-times mr-1"></i> Cancelar</a>
            <button type="submit" class="btn btn-primary"><i class="fas fa-save mr-1"></i> Guardar Configuración</button>
        </div>
    </form>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const logoInput = document.getElementById('logo');
    const logoPreview = document.getElementById('logo-preview');
    const customFileLabel = document.querySelector('.custom-file-label');

    logoInput.addEventListener('change', function(e) {
        const file = e.target.files[0];
        if (file) {
            customFileLabel.textContent = file.name;
            const reader = new FileReader();
            reader.onload = function(e) {
                logoPreview.src = e.target.result;
                logoPreview.style.display = 'inline-block';
            }
            reader.readAsDataURL(file);
        } else {
            customFileLabel.textContent = 'Seleccionar archivo...';
        }
    });
});
</script>
