<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=requisiciones_bienes/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nueva Requisición
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <?php if (isset($_SESSION['success'])): ?>
      <div class="alert alert-success alert-dismissible">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <h5><i class="icon fas fa-check"></i> Éxito</h5>
        <?= htmlspecialchars($_SESSION['success']) ?>
      </div>
      <?php unset($_SESSION['success']); ?>
    <?php endif; ?>

    <form method="GET" action="" class="form-inline mb-4">
      <input type="hidden" name="route" value="requisiciones_bienes/index">
      <div class="form-group mr-2">
        <input type="text" name="search" class="form-control" placeholder="Buscar Requisición..." value="<?= htmlspecialchars($search) ?>" style="width: 250px;">
      </div>
      <div class="form-group mr-2">
        <select name="mes" class="form-control">
          <option value="">Todos los Meses</option>
          <?php for($i=1; $i<=12; $i++): $m = str_pad((string)$i, 2, '0', STR_PAD_LEFT); ?>
            <option value="<?= htmlspecialchars((string)$m) ?>" <?= ($mes === $m) ? 'selected' : '' ?>>Mes <?= htmlspecialchars((string)$m) ?></option>
          <?php endfor; ?>
        </select>
      </div>
      <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
      <a href="?route=requisiciones_bienes/index" class="btn btn-default ml-2">Limpiar</a>
    </form>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100 table-sm">
        <thead class="thead-dark">
          <tr>
            <th class="text-center">Documento</th>
            <th class="text-center">Fecha</th>
            <th>Concepto</th>
            <th>Estructura Presupuestaria</th>
            <th class="text-center">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($requisiciones)): ?>
            <tr>
              <td colspan="5" class="text-center">No se encontraron requisiciones.</td>
            </tr>
          <?php else: ?>
            <?php foreach ($requisiciones as $data): ?>
              <?php 
                $req = $data['entity']; 
                $prog = $data['programatica'];
              ?>
              <tr>
                <td class="text-center align-middle"><strong><?= htmlspecialchars((string)$req->id) ?></strong></td>
                <td class="text-center align-middle"><?= htmlspecialchars($req->fecha) ?></td>
                <td class="align-middle"><?= htmlspecialchars($req->concepto) ?></td>
                <td class="align-middle"><?= htmlspecialchars($prog) ?></td>
                <td class="text-center align-middle text-nowrap">
                  <a href="?route=documental/requisicionBienes&id=<?= urlencode((string)$req->id) ?>" target="_blank" class="btn btn-sm btn-secondary" title="Imprimir PDF">
                    <i class="fas fa-file-pdf"></i>
                  </a>
                  <?= render_component('table_actions', [
                'editUrl' => '?route=requisiciones_bienes/form&id=' . $req->id,
                'deleteUrl' => '?route=requisiciones_bienes/eliminar',
                'deleteId' => $req->id
            ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
        </tbody>
      </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
    </div>
  </div>
</div>
