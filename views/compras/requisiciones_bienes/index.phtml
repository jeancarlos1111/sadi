<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="requisiciones_bienes/index">
        <input type="text" name="search" placeholder="Buscar Requisición..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 250px;">
        <select name="mes" style="padding: 5px;">
            <option value="">Todos los Meses</option>
            <?php for($i=1; $i<=12; $i++): $m = str_pad((string)$i, 2, '0', STR_PAD_LEFT); ?>
                <option value="<?= $m ?>" <?= ($mes === $m) ? 'selected' : '' ?>>Mes <?= $m ?></option>
            <?php endfor; ?>
        </select>
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=requisiciones_bienes/index" style="margin-left: 10px;">Limpiar</a>
    </form>
    <a href="?route=requisiciones_bienes/form" style="background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">+ Nueva Requisición</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>Documento</th>
            <th>Fecha</th>
            <th>Concepto</th>
            <th>Estructura Presupuestaria</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($requisiciones)): ?>
            <tr>
                <td colspan="5" style="text-align: center;">No se encontraron requisiciones.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($requisiciones as $data): ?>
                <?php 
                    $req = $data['entity']; 
                    $prog = $data['programatica'];
                ?>
                <tr>
                    <td><?= htmlspecialchars((string)$req->id) ?></td>
                    <td><?= htmlspecialchars($req->fecha) ?></td>
                    <td><?= htmlspecialchars($req->concepto) ?></td>
                    <td><?= htmlspecialchars($prog) ?></td>
                    <td>
                        <a href="?route=documental/requisicionBienes&id=<?= urlencode((string)$req->id) ?>" target="_blank" style="margin-right: 10px; color: #198754; font-weight: bold;">[ PDF ]</a>
                        <a href="?route=requisiciones_bienes/form&id=<?= urlencode((string)$req->id) ?>" style="margin-right: 10px;">Editar</a>
                        <form method="POST" action="?route=requisiciones_bienes/eliminar" onsubmit="return confirm('¿Seguro de eliminar esta requisición?');" style="display:inline;">
                            <input type="hidden" name="id" value="<?= htmlspecialchars((string)$req->id) ?>">
                            <button type="submit" style="color: red; border:none; background:none; cursor:pointer;">Eliminar</button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
