<div class="row">
    <div class="col-md-10 offset-md-1">
        <div class="card card-outline card-primary">
            <div class="card-header">
                <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
            </div>
            <div class="card-body">
                <?php if (isset($error)): ?>
                    <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
                <?php endif; ?>

                <form method="POST" action="?route=articulos/guardar">
                    <h5 class="mb-3 text-primary border-bottom pb-2">Información Principal del Bien/Servicio</h5>
                    
                    <?php if ($articulo && $articulo->id): ?>
                        <input type="hidden" name="id" value="<?= htmlspecialchars((string)$articulo->id) ?>">
                    <?php endif; ?>

                    <div class="form-group">
                        <label>Denominación <span class="text-danger">*</span></label>
                        <input type="text" name="denominacion" class="form-control" value="<?= htmlspecialchars($articulo->denominacion ?? '') ?>" required maxlength="50" placeholder="Ej: Resma de papel carta">
                    </div>

                    <div class="form-group">
                        <label>Observación</label>
                        <textarea name="observacion" class="form-control" rows="3" placeholder="Detalles adicionales..."><?= htmlspecialchars($articulo->observacion ?? '') ?></textarea>
                    </div>

                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Tipo de Artículo <span class="text-danger">*</span></label>
                                <select name="id_tipo_de_articulo" class="form-control" required>
                                    <option value="">-- SELECCIONE --</option>
                                    <?php foreach ($tiposArticulo as $tipo): ?>
                                        <option value="<?= htmlspecialchars((string)$tipo->id) ?>" <?= ($articulo && $articulo->idTipoDeArticulo === $tipo->id) ? 'selected' : '' ?>>
                                            <?= htmlspecialchars($tipo->denominacion) ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Unidad de Medida <span class="text-danger">*</span></label>
                                <select name="id_unidades_de_medida" class="form-control" required>
                                    <option value="">-- SELECCIONE --</option>
                                    <?php foreach ($unidadesMedida as $udm): ?>
                                        <option value="<?= htmlspecialchars((string)$udm->id) ?>" <?= ($articulo && $articulo->idUnidadesDeMedida === $udm->id) ? 'selected' : '' ?>>
                                            <?= htmlspecialchars($udm->denominacion) ?>
                                        </option>
                                    <?php endforeach; ?>
                                </select>
                            </div>
                        </div>
                    </div>

                    <div class="form-group">
                        <label>ID Código Plan Único (Cuenta)</label>
                        <select name="id_codigo_plan_unico" class="form-control select2" style="width: 100%;">
                            <option value="">-- NO ASOCIAR CUENTA ESPECÍFICA --</option>
                            <?php foreach ($cuentasContables as $cuenta): ?>
                                <option value="<?= htmlspecialchars((string)$cuenta->id) ?>" <?= ($articulo && $articulo->idCodigoPlanUnico === $cuenta->id) ? 'selected' : '' ?>>
                                    <?= htmlspecialchars($cuenta->codigoCuenta . ' - ' . $cuenta->denominacion) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                        <small class="form-text text-muted">Aplica si el artículo se asocia directamente a una cuenta contable o presupuestaria específica.</small>
                    </div>

                    <div class="form-group">
                        <div class="custom-control custom-checkbox border p-2 rounded bg-light">
                            <input class="custom-control-input" type="checkbox" id="aplicar_iva" name="aplicar_iva" value="1" <?= ($articulo && $articulo->aplicarIva) ? 'checked' : '' ?>>
                            <label for="aplicar_iva" class="custom-control-label font-weight-bold" style="cursor: pointer;">
                                ¿Aplica I.V.A.?
                            </label>
                            <span class="d-block text-muted small mt-1">Marque si este artículo está sujeto a retención de impuestos.</span>
                        </div>
                    </div>

                    <div class="mt-4 border-top pt-3 text-right">
                        <a href="?route=articulos/index" class="btn btn-default"><i class="fas fa-times"></i> Cancelar</a>
                        <button type="submit" class="btn btn-success ml-2"><i class="fas fa-save"></i> Guardar Artículo</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        if (typeof $ !== 'undefined' && $.fn.select2) {
            $('.select2').select2({
                theme: 'bootstrap4',
                placeholder: "-- NO ASOCIAR CUENTA ESPECÍFICA --",
                allowClear: true
            });
        }
    });
</script>
