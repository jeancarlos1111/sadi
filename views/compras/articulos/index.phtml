<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="articulos/index">
        <input type="text" name="search" placeholder="Buscar Artículo..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 300px;">
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=articulos/index" style="margin-left: 10px;">Limpiar</a>
    </form>
    <a href="?route=articulos/form" style="background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">+ Nuevo Artículo</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>ID</th>
            <th>Denominación</th>
            <th>Tipo</th>
            <th>Unidad de Medida</th>
            <th>Aplica IVA</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($articulos)): ?>
            <tr>
                <td colspan="6" style="text-align: center;">No se encontraron artículos.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($articulos as $data): ?>
                <?php 
                    $a = $data['entity']; 
                    $tipo = $data['denominacion_tda'];
                    $udm = $data['denominacion_udm'];
                ?>
                <tr>
                    <td><?= htmlspecialchars((string)$a->id) ?></td>
                    <td><?= htmlspecialchars($a->denominacion) ?></td>
                    <td><?= htmlspecialchars($tipo) ?></td>
                    <td><?= htmlspecialchars($udm) ?></td>
                    <td><?= $a->aplicarIva ? 'Sí' : 'No' ?></td>
                    <td>
                        <a href="?route=articulos/form&id=<?= urlencode((string)$a->id) ?>" style="margin-right: 10px;">Editar</a>
                        <form method="POST" action="?route=articulos/eliminar" onsubmit="return confirm('¿Seguro de eliminar este artículo?');" style="display:inline;">
                            <input type="hidden" name="id" value="<?= htmlspecialchars((string)$a->id) ?>">
                            <button type="submit" style="color: red; border:none; background:none; cursor:pointer;">Eliminar</button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
