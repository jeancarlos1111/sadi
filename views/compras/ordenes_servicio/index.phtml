<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=ordenes_servicio/form" class="btn btn-sm btn-success"><i class="fas fa-plus"></i> Nueva O/S</a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($_GET['success'])): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($_GET['success']) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($_GET['error'])): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($_GET['error']) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <form method="GET" action="" class="form-inline mb-4">
            <input type="hidden" name="route" value="ordenes_servicio/index">
            <div class="form-group mr-2">
                <input type="text" name="search" class="form-control" placeholder="Buscar O/S..." value="<?= htmlspecialchars($search) ?>" style="width: 220px;">
            </div>
            <div class="form-group mr-2">
                <select name="mes" class="form-control">
                    <option value="">Todos los Meses</option>
                    <?php for($i=1; $i<=12; $i++): $m = str_pad((string)$i, 2, '0', STR_PAD_LEFT); ?>
                        <option value="<?= $m ?>" <?= ($mes === $m) ? 'selected' : '' ?>>Mes <?= $m ?></option>
                    <?php endfor; ?>
                </select>
            </div>
            <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
            <a href="?route=ordenes_servicio/index" class="btn btn-default ml-2">Limpiar</a>
        </form>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th class="text-center">Nro.</th>
                        <th class="text-center">Fecha</th>
                        <th>Proveedor</th>
                        <th>Concepto</th>
                        <th class="text-center">Estado</th>
                        <th class="text-right">Monto Total</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($ordenes)): ?>
                        <tr>
                            <td colspan="7" class="text-center">No hay órdenes de servicio.</td>
                        </tr>
                    <?php else: foreach ($ordenes as $data): $os = $data['entity']; ?>
                        <tr>
                            <td class="text-center align-middle"><strong><?= $os->id ?></strong></td>
                            <td class="text-center align-middle"><?= htmlspecialchars($os->fecha) ?></td>
                            <td class="align-middle"><?= htmlspecialchars($data['proveedor']) ?></td>
                            <td class="align-middle"><?= htmlspecialchars($os->concepto) ?></td>
                            <td class="text-center align-middle">
                                <?php if ($os->contabilizada): ?>
                                    <span class="badge badge-success px-2 py-1"><i class="fas fa-check-circle"></i> Contabilizada</span>
                                <?php else: ?>
                                    <span class="badge badge-warning px-2 py-1"><i class="fas fa-clock"></i> Pendiente</span>
                                <?php endif; ?>
                            </td>
                            <td class="text-right align-middle font-weight-bold">
                                <?= number_format($os->montoTotal, 2, ',', '.') ?>
                            </td>
                            <td class="text-center align-middle text-nowrap">
                                <a href="?route=documental/ordenServicio&id=<?= $os->id ?>" target="_blank" class="btn btn-sm btn-secondary" title="Imprimir PDF">
                                    <i class="fas fa-file-pdf"></i>
                                </a>
                                <?php if (!$os->contabilizada): ?>
                                    <a href="?route=ordenes_servicio/form&id=<?= $os->id ?>" class="btn btn-sm btn-info" title="Editar">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form method="POST" action="?route=ordenes_servicio/contabilizar" style="display:inline;">
                                        <input type="hidden" name="id" value="<?= $os->id ?>">
                                        <button type="submit" onclick="return confirm('¿Contabilizar esta orden? Afectará el presupuesto.');" class="btn btn-sm btn-primary" title="Contabilizar">
                                            <i class="fas fa-check"></i>
                                        </button>
                                    </form>
                                    <form method="POST" action="?route=ordenes_servicio/eliminar" style="display:inline;">
                                        <input type="hidden" name="id" value="<?= $os->id ?>">
                                        <button type="submit" onclick="return confirm('¿Eliminar?');" class="btn btn-sm btn-danger" title="Eliminar">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                <?php else: ?>
                                    <form method="POST" action="?route=ordenes_servicio/reversar" style="display:inline;">
                                        <input type="hidden" name="id" value="<?= $os->id ?>">
                                        <button type="submit" onclick="return confirm('¿Reversar esta orden? Restaurará el presupuesto.');" class="btn btn-sm btn-warning" title="Reversar">
                                            <i class="fas fa-undo"></i>
                                        </button>
                                    </form>
                                <?php endif; ?>
                            </td>
                        </tr>
                    <?php endforeach; endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
<!-- Alerts display handled by PHP alert divs above now instead of JS alerts -->
