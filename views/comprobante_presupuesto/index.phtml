<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <?php foreach ($tipos as $cod => $label): ?>
                <a href="?route=comprobante_presupuesto/form&tipo=<?= urlencode($cod) ?>" class="btn btn-sm btn-success">
                   <i class="fas fa-plus"></i> <?= htmlspecialchars($cod) ?>
                </a>
            <?php endforeach; ?>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($success)): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($success) ?>
            </div>
        <?php endif; ?>
        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <div class="mb-3">
            <form method="GET" action="" class="form-inline">
                <input type="hidden" name="route" value="comprobante_presupuesto/index">
                <input type="text" class="form-control mr-2" name="search" placeholder="Buscar N° o denominación..." value="<?= htmlspecialchars($search) ?>">
                <select class="form-control mr-2" name="tipo">
                    <option value="">-- Todos los tipos --</option>
                    <?php foreach ($tipos as $cod => $label): ?>
                        <option value="<?= htmlspecialchars($cod) ?>" <?= $tipo_filtro === $cod ? 'selected' : '' ?>>
                            <?= htmlspecialchars($cod) ?> – <?= htmlspecialchars($label) ?>
                        </option>
                    <?php endforeach; ?>
                </select>
                <button type="submit" class="btn btn-primary mr-2"><i class="fas fa-search"></i> Buscar</button>
                <a href="?route=comprobante_presupuesto/index" class="btn btn-default">Limpiar</a>
            </form>
        </div>

        <table class="table table-bordered table-striped dataTable w-100">
            <thead>
                <tr>
                    <th>N° Comprobante</th>
                    <th>Tipo</th>
                    <th>Fecha</th>
                    <th>Denominación</th>
                    <th>Referencia</th>
                    <th>Beneficiario</th>
                    <th width="15%" class="text-center">Acciones</th>
                </tr>
            </thead>
            <tbody>
        <?php if (empty($items)): ?>
            <tr><td colspan="7" class="text-center">No hay comprobantes registrados.</td></tr>
        <?php else: ?>
            <?php foreach ($items as $c): ?>
                <tr>
                    <td><strong><?= htmlspecialchars($c->numero_c) ?></strong></td>
                    <td class="text-center">
                        <span class="badge badge-primary">
                            <?= htmlspecialchars($c->acronimo_c) ?>
                        </span>
                    </td>
                    <td class="text-center"><?= htmlspecialchars($c->fecha_c) ?></td>
                    <td><?= htmlspecialchars($c->denominacion_c) ?></td>
                    <td><?= htmlspecialchars($c->referencia_c ?? '—') ?></td>
                    <td><?= htmlspecialchars($c->beneficiario_cedula ?? '—') ?></td>
                    <td class="text-center text-nowrap">
                        <a href="?route=comprobante_presupuesto/form&id=<?= urlencode((string)$c->id_comprobante) ?>" class="btn btn-sm btn-info" title="Editar">
                            <i class="fas fa-edit"></i>
                        </a>
                        <form method="POST" action="?route=comprobante_presupuesto/eliminar"
                              onsubmit="return confirm('¿Eliminar el comprobante <?= htmlspecialchars($c->numero_c) ?>?');" class="d-inline">
                            <input type="hidden" name="id" value="<?= htmlspecialchars((string)$c->id_comprobante) ?>">
                            <button type="submit" class="btn btn-sm btn-danger" title="Eliminar">
                                <i class="fas fa-trash-alt"></i>
                            </button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
    </div>
</div>
