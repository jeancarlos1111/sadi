<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($_GET['error'])): ?>
    <div style="color: red; padding: 12px; border: 1px solid red; border-radius: 4px; margin-bottom: 20px;">
        <?= htmlspecialchars($_GET['error']) ?>
    </div>
<?php endif; ?>

<div style="background: #f8f9fa; padding: 20px; border: 1px solid #ddd; max-width: 600px;">
    <p style="margin-top: 0; color: #666; font-size: 0.9em; margin-bottom: 20px;">
        Esta opción generará un egreso bancario de tipo "Cheque" (CH). Asegúrese de imprimir el voucher después de emitirlo.
    </p>

    <form method="POST" action="?route=cheques/guardarDirecto">
        
        <div style="margin-bottom: 15px;">
            <label style="display: block; font-weight: bold; margin-bottom: 5px;">Cuenta Bancaria *</label>
            <select name="id_cta_bancaria" required style="padding: 8px; width: 100%; box-sizing: border-box;">
                <option value="">Seleccione una cuenta...</option>
                <?php foreach ($cuentas as $c): ?>
                    <option value="<?= $c['id_cta_bancaria'] ?>">
                        <?= htmlspecialchars($c['nombre_banco']) ?> - <?= htmlspecialchars($c['numero_cta_bancaria']) ?>
                    </option>
                <?php endforeach; ?>
            </select>
        </div>

        <div style="margin-bottom: 15px;">
            <label style="display: block; font-weight: bold; margin-bottom: 5px;">A la Orden De (Beneficiario) *</label>
            <input type="text" name="beneficiario_nombre" required style="padding: 8px; width: 100%; box-sizing: border-box;" maxlength="100" placeholder="Nombre completo o razón social">
            <div style="margin-top: 5px; font-size: 0.85em;">
                <label style="color: #0056b3; cursor: pointer;">
                    <input type="checkbox" id="chk_usar_maq"> O seleccionar del catálogo:
                </label>
                <select id="sel_beneficiario" style="padding: 4px; margin-left: 5px; max-width: 200px;">
                    <option value="">-- Seleccionar --</option>
                    <?php if (isset($beneficiarios)): ?>
                        <?php foreach($beneficiarios as $b): 
                           $nombreB = $b->nombres . ' ' . $b->apellidos;
                        ?>
                            <option value="<?= htmlspecialchars($nombreB) ?>"><?= htmlspecialchars($nombreB) ?> (<?= htmlspecialchars($b->cedula) ?>)</option>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </select>
                <script>
                    document.getElementById('sel_beneficiario').addEventListener('change', function() {
                        if (this.value !== '') {
                            document.querySelector('input[name="beneficiario_nombre"]').value = this.value;
                        }
                    });
                </script>
            </div>
        </div>

        <div style="display: flex; gap: 15px; margin-bottom: 15px;">
            <div style="flex: 1;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Monto (Bs) *</label>
                <input type="number" step="0.01" name="monto" required min="0.01" style="padding: 8px; width: 100%; box-sizing: border-box;">
            </div>
            <div style="flex: 1;">
                <label style="display: block; font-weight: bold; margin-bottom: 5px;">Nro. de Cheque *</label>
                <input type="text" name="referencia" required maxlength="20" style="padding: 8px; width: 100%; box-sizing: border-box;">
            </div>
        </div>

        <div style="margin-bottom: 15px;">
            <label style="display: block; font-weight: bold; margin-bottom: 5px;">Concepto</label>
            <textarea name="concepto" rows="3" style="padding: 8px; width: 100%; box-sizing: border-box;" placeholder="Motivo de la emisión..."></textarea>
        </div>

        <div style="margin-bottom: 15px;">
            <label style="display: block; font-weight: bold; margin-bottom: 5px;">Fecha de Emisión</label>
            <input type="date" name="fecha" required value="<?= htmlspecialchars($fechaActual) ?>" style="padding: 8px; width: 200px; box-sizing: border-box;">
        </div>

        <div style="margin-top: 20px; display: flex; justify-content: space-between;">
            <a href="?route=cheques/listado" style="color: #666; text-decoration: none; padding: 8px 15px;">Cancelar</a>
            <button type="submit" onclick="return confirm('¿Confirmar emisión definitiva del cheque?');" style="background-color: #007bff; color: white; border: none; padding: 10px 20px; border-radius: 4px; cursor: pointer; font-weight: bold;">Emitir Cheque</button>
        </div>
    </form>
</div>
