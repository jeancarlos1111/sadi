<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=banco/emitirPago" class="btn btn-sm btn-success font-weight-bold">
                <i class="fas fa-plus"></i> Emitir Pago Bancario
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($_GET['success'])): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($_GET['success']) ?>
            </div>
        <?php endif; ?>

        <div class="row mb-4">
            <div class="col-md-7">
                <div class="callout callout-info py-2 m-0 h-100">
                    <h5 class="mb-2"><i class="fas fa-building"></i> Listado de Cuentas Activas</h5>
                    <ul class="list-unstyled mb-0 pl-3">
                        <?php foreach ($cuentas as $cta): ?>
                            <li><i class="fas fa-wallet text-muted mr-1"></i> <strong><?= htmlspecialchars($cta->nombreBanco) ?>:</strong> <?= htmlspecialchars($cta->numeroCuenta) ?></li>
                        <?php endforeach; ?>
                    </ul>
                </div>
            </div>
            <div class="col-md-5">
                <form method="GET" action="" class="h-100 d-flex flex-column justify-content-center">
                    <input type="hidden" name="route" value="banco/index">
                    <label>Buscar Movimientos</label>
                    <div class="input-group">
                        <input type="text" class="form-control" name="search" placeholder="Ref, Cta o Banco..." value="<?= htmlspecialchars($search) ?>">
                        <span class="input-group-append">
                            <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
                            <a href="?route=banco/index" class="btn btn-default">Limpiar</a>
                        </span>
                    </div>
                </form>
            </div>
        </div>

        <h4 class="mb-3"><i class="fas fa-exchange-alt"></i> Movimientos Recientes</h4>
        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Fecha</th>
                        <th>Banco y Cuenta</th>
                        <th>Tipo / Ref</th>
                        <th class="text-right">Monto</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($movimientos)): ?>
                        <tr><td colspan="5" class="text-center py-4">No hay movimientos registrados.</td></tr>
                    <?php else: ?>
                        <?php foreach ($movimientos as $data): ?>
                            <?php $mov = $data['entity']; ?>
                            <tr>
                                <td class="text-nowrap"><?= htmlspecialchars($mov->fecha) ?></td>
                                <td>
                                    <?= htmlspecialchars($data['banco']) ?><br>
                                    <small class="text-muted"><?= htmlspecialchars($data['cuenta_bancaria']) ?></small>
                                </td>
                                <td>
                                    <strong><?= htmlspecialchars($data['operacion_acronimo']) ?></strong> - <?= htmlspecialchars($data['operacion_nombre']) ?><br>
                                    <small class="text-info font-weight-bold">Ref: <?= htmlspecialchars($mov->referencia) ?></small>
                                </td>
                                <td class="text-right font-weight-bold align-middle <?= $mov->monto >= 0 ? 'text-success' : 'text-danger' ?>">
                                    <?= number_format($mov->monto, 2, ',', '.') ?>
                                </td>
                                <td class="text-center align-middle">
                                    <?php if (isset($mov->idSolicitudPago) && $mov->idSolicitudPago): ?>
                                        <form method="POST" action="?route=banco/reversar" onsubmit="return confirm('¿Está seguro de reversar este pago?');" class="m-0">
                                            <input type="hidden" name="id_solicitud" value="<?= $mov->idSolicitudPago ?>">
                                            <button type="submit" class="btn btn-sm btn-outline-danger" title="Reversar Pago">
                                                <i class="fas fa-undo"></i> Reversar
                                            </button>
                                        </form>
                                    <?php else: ?>
                                        <span class="badge badge-secondary"><i class="fas fa-cogs"></i> Sistema</span>
                                    <?php endif; ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
