<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=tipos_operaciones_bancarias/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nuevo Tipo de Operación
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($success)): ?>
      <?= render_component('alert', ['type' => 'success', 'message' => $success]) ?>
    <?php endif; ?>

    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100 table-sm">
        <thead class="thead-dark">
          <tr>
            <th width="15%">Acrónimo</th>
            <th>Nombre de la Operación</th>
            <th class="text-center" width="15%">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($tipos)): ?>
            <tr><td colspan="3" class="text-center">No hay tipos de operación registrados.</td></tr>
          <?php else: ?>
            <?php foreach ($tipos as $t): ?>
              <tr>
                <td><span class="badge badge-info"><?= htmlspecialchars($t->acronimo) ?></span></td>
                <td><?= htmlspecialchars($t->nombre) ?></td>
                <td class="text-center text-nowrap">
                  <?= render_component('table_actions', [
                'editUrl' => '?route=tipos_operaciones_bancarias/form&id=' . $t->id,
                'deleteUrl' => '?route=tipos_operaciones_bancarias/eliminar',
                'deleteId' => $t->id
            ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
        </tbody>
      </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
    </div>
  </div>
</div>
