<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=tipos_operaciones_bancarias/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Nuevo Tipo de Operación
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($success)): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($success) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th width="15%">Acrónimo</th>
                        <th>Nombre de la Operación</th>
                        <th class="text-center" width="15%">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($tipos)): ?>
                        <tr><td colspan="3" class="text-center">No hay tipos de operación registrados.</td></tr>
                    <?php else: ?>
                        <?php foreach ($tipos as $t): ?>
                            <tr>
                                <td><span class="badge badge-info"><?= htmlspecialchars($t->acronimo) ?></span></td>
                                <td><?= htmlspecialchars($t->nombre) ?></td>
                                <td class="text-center text-nowrap">
                                    <a href="?route=tipos_operaciones_bancarias/form&id=<?= $t->id ?>" class="btn btn-sm btn-info" title="Editar">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form method="POST" action="?route=tipos_operaciones_bancarias/eliminar" class="d-inline" onsubmit="return confirm('¿Está seguro de eliminar este tipo de operación?');">
                                        <input type="hidden" name="id" value="<?= $t->id ?>">
                                        <button type="submit" class="btn btn-sm btn-danger" title="Eliminar">
                                            <i class="fas fa-trash-alt"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
