<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=bancos/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nuevo Banco
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($success)): ?>
      <?= render_component('alert', ['type' => 'success', 'message' => $success]) ?>
    <?php endif; ?>

    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100 table-sm">
        <thead class="thead-dark">
          <tr>
            <th class="text-center" width="10%">ID</th>
            <th>Nombre del Banco</th>
            <th class="text-center" width="15%">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($bancos)): ?>
            <tr><td colspan="3" class="text-center">No hay bancos registrados.</td></tr>
          <?php else: ?>
            <?php foreach ($bancos as $b): ?>
              <tr>
                <td class="text-center font-weight-bold"><?= htmlspecialchars((string)$b->id) ?></td>
                <td><?= htmlspecialchars($b->nombreBanco) ?></td>
                <td class="text-center text-nowrap">
                  <?= render_component('table_actions', [
                'editUrl' => '?route=bancos/form&id=' . $b->id,
                'deleteUrl' => '?route=bancos/eliminar',
                'deleteId' => $b->id
            ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
        </tbody>
      </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
    </div>
  </div>
</div>
