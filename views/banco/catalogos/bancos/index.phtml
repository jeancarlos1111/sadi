<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=bancos/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Nuevo Banco
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($success)): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($success) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th class="text-center" width="10%">ID</th>
                        <th>Nombre del Banco</th>
                        <th class="text-center" width="15%">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($bancos)): ?>
                        <tr><td colspan="3" class="text-center">No hay bancos registrados.</td></tr>
                    <?php else: ?>
                        <?php foreach ($bancos as $b): ?>
                            <tr>
                                <td class="text-center font-weight-bold"><?= $b->id ?></td>
                                <td><?= htmlspecialchars($b->nombreBanco) ?></td>
                                <td class="text-center text-nowrap">
                                    <a href="?route=bancos/form&id=<?= $b->id ?>" class="btn btn-sm btn-info" title="Editar">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form method="POST" action="?route=bancos/eliminar" class="d-inline" onsubmit="return confirm('¿Está seguro de eliminar este banco?');">
                                        <input type="hidden" name="id" value="<?= $b->id ?>">
                                        <button type="submit" class="btn btn-sm btn-danger" title="Eliminar">
                                            <i class="fas fa-trash-alt"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
