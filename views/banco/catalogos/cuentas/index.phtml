<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=cuentas_bancarias/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nueva Cuenta Bancaria
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($success)): ?>
      <?= render_component('alert', ['type' => 'success', 'message' => $success]) ?>
    <?php endif; ?>

    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100 table-sm">
        <thead class="thead-dark">
          <tr>
            <th>Banco</th>
            <th>Número de Cuenta</th>
            <th class="text-center" width="15%">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($cuentas)): ?>
            <tr><td colspan="3" class="text-center">No hay cuentas bancarias registradas.</td></tr>
          <?php else: ?>
            <?php foreach ($cuentas as $c): ?>
              <tr>
                <td><?= htmlspecialchars($c->nombreBanco) ?></td>
                <td class="font-weight-bold text-primary"><?= htmlspecialchars($c->numeroCuenta) ?></td>
                <td class="text-center text-nowrap">
                  <?= render_component('table_actions', [
                'editUrl' => '?route=cuentas_bancarias/form&id=' . $c->id,
                'deleteUrl' => '?route=cuentas_bancarias/eliminar',
                'deleteId' => $c->id
            ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
        </tbody>
      </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
    </div>
  </div>
</div>
