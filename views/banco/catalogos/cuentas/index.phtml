<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=cuentas_bancarias/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Nueva Cuenta Bancaria
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($success)): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-check"></i> Éxito</h5>
                <?= htmlspecialchars($success) ?>
            </div>
        <?php endif; ?>

        <?php if (isset($error)): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                <h5><i class="icon fas fa-ban"></i> Error</h5>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Banco</th>
                        <th>Número de Cuenta</th>
                        <th class="text-center" width="15%">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($cuentas)): ?>
                        <tr><td colspan="3" class="text-center">No hay cuentas bancarias registradas.</td></tr>
                    <?php else: ?>
                        <?php foreach ($cuentas as $c): ?>
                            <tr>
                                <td><?= htmlspecialchars($c->nombreBanco) ?></td>
                                <td class="font-weight-bold text-primary"><?= htmlspecialchars($c->numeroCuenta) ?></td>
                                <td class="text-center text-nowrap">
                                    <a href="?route=cuentas_bancarias/form&id=<?= $c->id ?>" class="btn btn-sm btn-info" title="Editar">
                                        <i class="fas fa-edit"></i>
                                    </a>
                                    <form method="POST" action="?route=cuentas_bancarias/eliminar" class="d-inline" onsubmit="return confirm('¿Está seguro de eliminar esta cuenta?');">
                                        <input type="hidden" name="id" value="<?= $c->id ?>">
                                        <button type="submit" class="btn btn-sm btn-danger" title="Eliminar">
                                            <i class="fas fa-trash-alt"></i>
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
