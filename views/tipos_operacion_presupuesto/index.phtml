<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=tipos_operacion_presupuesto/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Nuevo Tipo
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <table class="table table-bordered table-striped w-100">
      <thead class="thead-dark">
        <tr>
          <th>Denominación</th>
          <th>Descripción</th>
          <th width="15%">Acciones</th>
        </tr>
      </thead>
      <tbody>
    <?php if (empty($items)): ?>
      <tr><td colspan="3" style="text-align:center;">No hay tipos de operación registrados.</td></tr>
    <?php else: foreach ($items as $item): ?>
      <tr>
        <td><?= htmlspecialchars($item->denominacion) ?></td>
        <td><?= htmlspecialchars($item->descripcion ?? '') ?></td>
        <td class="text-center">
          <?= render_component('table_actions', [
                'editUrl' => '?route=tipos_operacion_presupuesto/form&id=' . $item->id,
                'deleteUrl' => '?route=tipos_operacion_presupuesto/eliminar',
                'deleteId' => $item->id
            ]) ?>
        </td>
      </tr>
    <?php endforeach; endif; ?>
      </tbody>
    </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
