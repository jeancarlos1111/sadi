<div class="card card-outline card-primary" style="max-width: 600px; margin: 0 auto;">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    </div>
    <form method="POST" action="?route=tipos_operacion_presupuesto/guardar">
        <div class="card-body">
            <?php if (isset($error)): ?>
                <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
            <?php endif; ?>

            <?php if ($item && $item->id): ?>
                <input type="hidden" name="id" value="<?= htmlspecialchars((string)$item->id) ?>">
            <?php endif; ?>

            <div class="form-group">
                <label>Denominación (*)</label>
                <input type="text" class="form-control" name="denominacion" value="<?= htmlspecialchars($item->denominacion ?? '') ?>" required maxlength="100" placeholder="Ingrese denominación">
            </div>
            
            <div class="form-group">
                <label>Descripción</label>
                <input type="text" class="form-control" name="descripcion" value="<?= htmlspecialchars($item->descripcion ?? '') ?>" maxlength="200" placeholder="Ingrese descripción">
            </div>
        </div>
        
        <div class="card-footer">
            <button type="submit" class="btn btn-success"><i class="fas fa-save"></i> Guardar</button>
            <a href="?route=tipos_operacion_presupuesto/index" class="btn btn-default float-right">Cancelar</a>
        </div>
    </form>
</div>
