<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color:red; padding:10px; border:1px solid red; margin-bottom:20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom:20px; display:flex; justify-content:space-between; align-items:center;">
    <form method="GET" action="" style="margin:0;">
        <input type="hidden" name="route" value="beneficiarios/index">
        <input type="text" name="search" placeholder="Buscar por cédula o nombre..." value="<?= htmlspecialchars($search) ?>" style="padding:5px; width:300px;">
        <button type="submit" style="padding:5px;">Buscar</button>
        <a href="?route=beneficiarios/index" style="margin-left:10px;">Limpiar</a>
    </form>
    <a href="?route=beneficiarios/form" style="background-color:#28a745; color:white; padding:10px 15px; text-decoration:none; border-radius:4px;">+ Nuevo Beneficiario</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width:100%; border-collapse:collapse;">
    <thead style="background-color:#0056b3; color:white;">
        <tr>
            <th>Cédula</th>
            <th>Nombres</th>
            <th>Apellidos</th>
            <th>Teléfono</th>
            <th>Email</th>
            <th>Dirección</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($beneficiarios)): ?>
            <tr><td colspan="7" style="text-align:center;">No se encontraron beneficiarios.</td></tr>
        <?php else: ?>
            <?php foreach ($beneficiarios as $b): ?>
                <tr>
                    <td><?= htmlspecialchars($b->cedula) ?></td>
                    <td><?= htmlspecialchars($b->nombres) ?></td>
                    <td><?= htmlspecialchars($b->apellidos) ?></td>
                    <td><?= htmlspecialchars($b->telefono) ?></td>
                    <td><?= htmlspecialchars($b->email ?? '') ?></td>
                    <td><?= htmlspecialchars($b->direccion) ?></td>
                    <td>
                        <a href="?route=beneficiarios/form&id=<?= urlencode((string)$b->id) ?>" style="margin-right:10px;">Editar</a>
                        <form method="POST" action="?route=beneficiarios/eliminar" onsubmit="return confirm('¿Eliminar este beneficiario?');" style="display:inline;">
                            <input type="hidden" name="id" value="<?= htmlspecialchars((string)$b->id) ?>">
                            <button type="submit" style="color:red; border:none; background:none; cursor:pointer;">Eliminar</button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
