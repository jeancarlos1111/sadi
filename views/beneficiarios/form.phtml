<div class="card card-outline card-primary" style="max-width: 600px; margin: 0 auto;">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    </div>
    <form method="POST" action="?route=beneficiarios/guardar">
        <div class="card-body">
            <?php if (isset($error)): ?>
                <div class="alert alert-danger alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                    <h5><i class="icon fas fa-ban"></i> Error</h5>
                    <?= htmlspecialchars($error) ?>
                </div>
            <?php endif; ?>

            <?php if ($beneficiario && $beneficiario->id): ?>
                <input type="hidden" name="id" value="<?= htmlspecialchars((string)$beneficiario->id) ?>">
            <?php endif; ?>

            <div class="form-group">
                <label>Cédula (*)</label>
                <input type="text" class="form-control" name="cedula" value="<?= htmlspecialchars($beneficiario->cedula ?? '') ?>" required maxlength="20" placeholder="Ej: V-12345678">
            </div>

            <div class="form-group">
                <label>Nombres (*)</label>
                <input type="text" class="form-control" name="nombres" value="<?= htmlspecialchars($beneficiario->nombres ?? '') ?>" required maxlength="100">
            </div>

            <div class="form-group">
                <label>Apellidos (*)</label>
                <input type="text" class="form-control" name="apellidos" value="<?= htmlspecialchars($beneficiario->apellidos ?? '') ?>" required maxlength="100">
            </div>

            <div class="form-group">
                <label>Dirección (*)</label>
                <textarea class="form-control" name="direccion" required rows="2"><?= htmlspecialchars($beneficiario->direccion ?? '') ?></textarea>
            </div>

            <div class="form-group">
                <label>Teléfono</label>
                <input type="text" class="form-control" name="telefono" value="<?= htmlspecialchars($beneficiario->telefono ?? '') ?>" maxlength="30" placeholder="Ej: 0414-1234567">
            </div>

            <div class="form-group">
                <label>Email</label>
                <input type="email" class="form-control" name="email" value="<?= htmlspecialchars($beneficiario->email ?? '') ?>" maxlength="100">
            </div>

            <div class="form-group">
                <label>ID Código Contable</label>
                <input type="number" class="form-control" name="id_codigo_contable" value="<?= htmlspecialchars((string)($beneficiario->idCodigoContable ?? '')) ?>">
            </div>
        </div>

        <div class="card-footer">
            <button type="submit" class="btn btn-success"><i class="fas fa-save"></i> Guardar Beneficiario</button>
            <a href="?route=beneficiarios/index" class="btn btn-default float-right">Cancelar</a>
        </div>
    </form>
</div>
