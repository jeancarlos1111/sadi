<?php
/** @var string $titulo */
/** @var array $fondos */
/** @var string $search */
/** @var string|null $error */
/** @var string|null $success */
?>
<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=fondo_avance/form" class="btn btn-sm btn-success">
                <i class="fas fa-plus"></i> Nuevo Fondo
            </a>
        </div>
    </div>
    
    <div class="card-body">
        <?php if ($error): ?>
            <div class="alert alert-danger alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                <?= htmlspecialchars($error) ?>
            </div>
        <?php endif; ?>
        <?php if ($success): ?>
            <div class="alert alert-success alert-dismissible">
                <button type="button" class="close" data-dismiss="alert" aria-hidden="true">×</button>
                <?= htmlspecialchars($success) ?>
            </div>
        <?php endif; ?>

        <div class="table-responsive">
            <table class="table table-bordered table-striped dataTable w-100">
                <thead class="thead-dark">
                    <tr>
                        <th>N°</th>
                        <th>Denominación</th>
                        <th>Responsable C.I.</th>
                        <th>Monto Máximo</th>
                        <th>Creación</th>
                        <th>Estado</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($fondos)): ?>
                        <tr><td colspan="7" class="text-center">No hay fondos en avance registrados.</td></tr>
                    <?php else: ?>
                        <?php foreach ($fondos as $item): ?>
                            <tr>
                                <td>F-<?= str_pad((string)$item['id_fondo'], 4, '0', STR_PAD_LEFT) ?></td>
                                <td><?= htmlspecialchars($item['denominacion']) ?></td>
                                <td><?= htmlspecialchars($item['responsable_cedula']) ?></td>
                                <td class="text-right"><?= number_format((float)$item['monto_maximo'], 2, ',', '.') ?></td>
                                <td><?= date('d/m/Y', strtotime($item['fecha_creacion'])) ?></td>
                                <td class="text-center">
                                    <span class="badge <?= $item['estado'] === 'ACTIVO' ? 'badge-success' : 'badge-danger' ?>">
                                        <?= $item['estado'] ?>
                                    </span>
                                </td>
                                <td class="text-center">
                                    <a href="?route=fondo_avance/reposiciones&id_fondo=<?= $item['id_fondo'] ?>" class="btn btn-sm btn-info" title="Rendiciones y Reposiciones">
                                        <i class="fas fa-money-check-alt"></i> Rendiciones
                                    </a>
                                    <?= render_component('table_actions', [
                                        'editUrl' => '?route=fondo_avance/form&id=' . $item['id_fondo']
                                    ]) ?>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        </div>
    </div>
</div>
