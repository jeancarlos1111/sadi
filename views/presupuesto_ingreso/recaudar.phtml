<div class="row">
    <div class="col-md-8 offset-md-2">
        <div class="card card-outline card-success">
            <div class="card-header">
                <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
            </div>
            <form method="POST" action="?route=presupuestoIngreso/procesarRecaudacion">
                <div class="card-body">
                    <div class="alert alert-warning">
                        <h5><i class="icon fas fa-exclamation-triangle"></i> Atención</h5>
                        <p>Registrar la entrada de efectivo o crédito a las arcas de la institución. </p>
                        <p class="mb-0"><b>¡ATENCIÓN!</b> Esta acción afectará el presupuesto ejecutado (Ingresado) e insertará automáticamente el cruzamiento contable en Libros Diarios.</p>
                    </div>

                    <div class="form-group">
                        <label>Abonar a (Ramo de Ingreso Pre-Formulado):</label>
                        <select name="id_presupuesto_ingreso" class="form-control" required>
                            <option value="">Seleccione dónde se deposita lo recaudado...</option>
                            <?php foreach ($formulado as $f): ?>
                                <option value="<?= $f['id_presupuesto_ingreso'] ?>">
                                    <?= htmlspecialchars($f['codigo_ramo'] . ' - ' . $f['denominacion_ramo']) ?>
                                </option>
                            <?php endforeach; ?>
                        </select>
                        <small class="form-text text-muted">Seleccione un ramo que ya cuente con saldo/crédito estimado formulado.</small>
                    </div>

                    <div class="row">
                        <div class="col-sm-6">
                            <div class="form-group">
                                <label>Monto Recibido/Liquidado (Bs):</label>
                                <input type="number" class="form-control text-success font-weight-bold" step="0.01" min="1" name="monto_recaudado" required placeholder="Ej: 20000.00">
                            </div>
                        </div>
                        <div class="col-sm-6">
                            <div class="form-group">
                                <label>Referencia Bancaria/OFIC:</label>
                                <input type="text" class="form-control" name="referencia" placeholder="Ref. Transferencia..." required>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card-footer text-right">
                    <a href="?route=presupuestoIngreso/index" class="btn btn-default mr-2">Cancelar</a>
                    <button type="submit" class="btn btn-success">
                        <i class="fas fa-check-circle"></i> Confirmar y Contabilizar Ingreso
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
