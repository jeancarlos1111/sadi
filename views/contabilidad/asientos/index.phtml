<div class="card card-outline card-primary">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
        <div class="card-tools">
            <a href="?route=contabilidad/planCuentas" class="btn btn-sm btn-info">
                <i class="fas fa-list-ol"></i> Ver Plan de Cuentas
            </a>
            <a href="?route=contabilidad/form" class="btn btn-sm btn-success ml-1 font-weight-bold">
                <i class="fas fa-plus"></i> Emitir Comprobante
            </a>
        </div>
    </div>
    <div class="card-body">
        <?php if (isset($error)): ?>
            <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
        <?php endif; ?>

        <form method="GET" action="" class="form-inline mb-4">
            <input type="hidden" name="route" value="contabilidad/asientos">
            <div class="input-group mr-2">
                <input type="text" class="form-control" name="search" placeholder="Nº Comprobante o Concepto..." value="<?= htmlspecialchars($search) ?>" style="width: 300px;">
                <span class="input-group-append">
                    <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
                    <a href="?route=contabilidad/asientos" class="btn btn-default">Limpiar</a>
                </span>
            </div>
        </form>

        <div class="table-responsive">
            <table class="table table-bordered table-striped w-100 table-sm">
                <thead class="thead-dark">
                    <tr>
                        <th>Fecha</th>
                        <th>Nº Comprobante</th>
                        <th>Concepto del Asiento</th>
                        <th class="text-right">Total Debe</th>
                        <th class="text-right">Total Haber</th>
                        <th class="text-center">Acciones</th>
                    </tr>
                </thead>
                <tbody>
                    <?php if (empty($asientos)): ?>
                        <tr><td colspan="6" class="text-center">No hay comprobantes de diario registrados.</td></tr>
                    <?php else: ?>
                        <?php foreach ($asientos as $asiento): ?>
                            <tr>
                                <td class="text-nowrap"><?= htmlspecialchars($asiento->fecha) ?></td>
                                <td class="font-weight-bold text-primary"><?= htmlspecialchars($asiento->numeroComprobante) ?></td>
                                <td><?= htmlspecialchars($asiento->concepto) ?></td>
                                <td class="text-right text-primary font-weight-bold">
                                    <?= number_format($asiento->totalDebe, 2, ',', '.') ?>
                                </td>
                                <td class="text-right text-danger font-weight-bold">
                                    <?= number_format($asiento->totalHaber, 2, ',', '.') ?>
                                </td>
                                <td class="text-center">
                                    <a href="?route=documental/comprobanteDiario&id=<?= urlencode((string)$asiento->id) ?>" target="_blank" class="btn btn-sm btn-outline-success font-weight-bold" title="Ver PDF">
                                        <i class="fas fa-file-pdf"></i> PDF
                                    </a>
                                    <form method="POST" action="?route=contabilidad/anular" class="d-inline" onsubmit="return confirm('¿Está seguro de anular este comprobante de diario? (Solo aplicará reversión lógica si la norma lo permite)');">
                                        <input type="hidden" name="id" value="<?= htmlspecialchars((string)$asiento->id) ?>">
                                        <button type="submit" class="btn btn-sm btn-outline-danger font-weight-bold" title="Anular Asiento">
                                            <i class="fas fa-ban"></i> Anular
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        <?php endforeach; ?>
                    <?php endif; ?>
                </tbody>
            </table>
        <?php if (isset($paginator)): ?>
            <?php include $partialsPath . 'pagination.phtml'; ?>
        <?php endif; ?>
        </div>
    </div>
</div>
