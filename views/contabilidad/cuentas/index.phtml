<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="contabilidad/planCuentas">
        <input type="text" name="search" placeholder="Código o Denominación..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 300px;">
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=contabilidad/planCuentas" style="margin-left: 10px;">Limpiar</a>
    </form>
    <div>
        <a href="?route=contabilidad/asientos" style="background-color: #6c757d; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">Ir a Comprobantes de Diario</a>
    </div>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>Código Contable</th>
            <th>Denominación</th>
            <th>Tipo de Cuenta</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($cuentas)): ?>
            <tr>
                <td colspan="3" style="text-align: center;">No hay cuentas contables registradas.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($cuentas as $cuenta): ?>
                <tr>
                    <td style="font-family: monospace; font-size: 1.1em; font-weight: bold;"><?= htmlspecialchars($cuenta->codigoCuenta) ?></td>
                    <td><?= htmlspecialchars($cuenta->denominacion) ?></td>
                    <td><?= htmlspecialchars($cuenta->tipoCuenta) ?></td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
