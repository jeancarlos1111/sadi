<div class="card card-outline card-primary" style="max-width: 600px; margin: 0 auto;">
    <div class="card-header">
        <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    </div>
    <form method="POST" action="?route=proveedores/guardar">
        <div class="card-body">
            <?php if (isset($error)): ?>
                <div class="alert alert-danger alert-dismissible">
                    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                    <h5><i class="icon fas fa-ban"></i> Error</h5>
                    <?= htmlspecialchars($error) ?>
                </div>
            <?php endif; ?>

            <?php if ($proveedor && $proveedor->id): ?>
                <input type="hidden" name="id" value="<?= htmlspecialchars((string)$proveedor->id) ?>">
            <?php endif; ?>

            <div class="form-group">
                <label>RIF (*)</label>
                <input type="text" class="form-control" name="rif" value="<?= htmlspecialchars($proveedor->rif ?? '') ?>" required maxlength="15">
            </div>

            <div class="form-group">
                <label>NIT</label>
                <input type="text" class="form-control" name="nit" value="<?= htmlspecialchars($proveedor->nit ?? '') ?>" maxlength="15">
            </div>

            <div class="form-group">
                <label>Compañía (*)</label>
                <input type="text" class="form-control" name="compania" value="<?= htmlspecialchars($proveedor->compania ?? '') ?>" required maxlength="60">
            </div>

            <div class="form-group">
                <label>Tipo Organización (*)</label>
                <select class="form-control" name="id_tipo_organizacion" required>
                    <option value="">SELECCIONE</option>
                    <?php foreach ($tiposOrganizacion as $tipo): ?>
                        <option value="<?= $tipo->id ?>" <?= ($proveedor && $proveedor->idTipoOrganizacion === $tipo->id) ? 'selected' : '' ?>>
                            <?= htmlspecialchars($tipo->nombre) ?>
                        </option>
                    <?php endforeach; ?>
                </select>
            </div>

            <div class="form-group">
                <label>Dirección (*)</label>
                <textarea class="form-control" name="direccion" required rows="3"><?= htmlspecialchars($proveedor->direccion ?? '') ?></textarea>
            </div>

            <div class="form-group">
                <label>Teléfono</label>
                <input type="text" class="form-control" name="telefono" value="<?= htmlspecialchars($proveedor->telefono ?? '') ?>" maxlength="30">
            </div>

            <div class="form-group">
                <label>ID Código Contable</label>
                <input type="number" class="form-control" name="id_codigo_contable" value="<?= htmlspecialchars((string)($proveedor->idCodigoContable ?? '')) ?>">
            </div>
        </div>

        <div class="card-footer">
            <button type="submit" class="btn btn-success"><i class="fas fa-save"></i> Guardar Proveedor</button>
            <a href="?route=proveedores/index" class="btn btn-default float-right">Cancelar</a>
        </div>
    </form>
</div>
