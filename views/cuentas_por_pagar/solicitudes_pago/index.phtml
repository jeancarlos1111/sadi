<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="solicitudes_pago/index">
        <input type="text" name="search" placeholder="Buscar Solicitud..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 250px;">
        <select name="mes" style="padding: 5px;">
            <option value="">Todos los Meses</option>
            <?php for($i=1; $i<=12; $i++): $m = str_pad((string)$i, 2, '0', STR_PAD_LEFT); ?>
                <option value="<?= $m ?>" <?= ($mes === $m) ? 'selected' : '' ?>>Mes <?= $m ?></option>
            <?php endfor; ?>
        </select>
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=solicitudes_pago/index" style="margin-left: 10px;">Limpiar</a>
    </form>
    <a href="?route=solicitudes_pago/form" style="background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">+ Nueva Solicitud</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>Documento Nº</th>
            <th>Fecha</th>
            <th>Concepto</th>
            <th>Monto a Pagar</th>
            <th>Estado</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($solicitudes)): ?>
            <tr>
                <td colspan="6" style="text-align: center;">No se encontraron solicitudes de pago.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($solicitudes as $data): ?>
                <?php $sp = $data['entity']; ?>
                <tr>
                    <td><?= htmlspecialchars((string)$sp->id) ?></td>
                    <td><?= htmlspecialchars($sp->fecha) ?></td>
                    <td><?= htmlspecialchars($sp->concepto) ?></td>
                    <td><?= number_format($sp->montoPagar, 2, ',', '.') ?></td>
                    <td style="font-weight: bold; color: <?= $sp->estado === 'Aprobada/Contabilizada' ? 'green' : 'orange' ?>;">
                        <?= htmlspecialchars($sp->estado) ?>
                    </td>
                    <td>
                        <a href="?route=documental/solicitudPago&id=<?= urlencode((string)$sp->id) ?>" target="_blank" style="margin-right: 10px; color: #198754; font-weight: bold;">[ PDF ]</a>
                        
                        <?php if ($sp->estado !== 'Aprobada/Contabilizada'): ?>
                            <a href="?route=solicitudes_pago/pagar&id=<?= urlencode((string)$sp->id) ?>" style="color: green; text-decoration: none; font-weight: bold; margin-right: 10px;">Pagar / Emitir</a>
                            <a href="?route=solicitudes_pago/desprogramar&id=<?= urlencode((string)$sp->id) ?>" onclick="return confirm('¿Está seguro de DESPROGRAMAR (detener y anular) esta solicitud de pago?');" style="color: #dc3545; text-decoration: none; font-weight: bold; margin-right: 10px;">[ Desprogramar ]</a>
                        <?php else: ?>
                            <a href="?route=solicitudes_pago/reversar&id=<?= urlencode((string)$sp->id) ?>" 
                               onclick="return confirm('¿Está seguro de REVERSAR este pago? Se eliminarán los movimientos de banco y contabilidad, y se restaurará el presupuesto.')"
                               style="color: #dc3545; text-decoration: none; font-weight: bold; margin-right: 10px;">[ Reversar ]</a>
                        <?php endif; ?>
                        
                        <a href="?route=solicitudes_pago/form&id=<?= urlencode((string)$sp->id) ?>" style="margin-right: 10px;">Editar</a>
                        <form method="POST" action="?route=solicitudes_pago/eliminar" onsubmit="return confirm('¿Seguro de eliminar esta solicitud?');" style="display:inline;">
                            <input type="hidden" name="id" value="<?= htmlspecialchars((string)$sp->id) ?>">
                            <button type="submit" style="color: red; border:none; background:none; cursor:pointer;">Eliminar</button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
