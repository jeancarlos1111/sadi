<h2><?= htmlspecialchars($titulo) ?></h2>

<?php if (isset($error)): ?>
    <div style="color: red; padding: 10px; border: 1px solid red; margin-bottom: 20px;">
        <?= htmlspecialchars($error) ?>
    </div>
<?php endif; ?>

<div style="margin-bottom: 20px; display: flex; justify-content: space-between; align-items: center;">
    <form method="GET" action="" style="margin: 0;">
        <input type="hidden" name="route" value="documentos_por_pagar/index">
        <input type="text" name="search" placeholder="Buscar CxP..." value="<?= htmlspecialchars($search) ?>" style="padding: 5px; width: 250px;">
        <select name="mes" style="padding: 5px;">
            <option value="">Todos los Meses</option>
            <?php for($i=1; $i<=12; $i++): $m = str_pad((string)$i, 2, '0', STR_PAD_LEFT); ?>
                <option value="<?= $m ?>" <?= ($mes === $m) ? 'selected' : '' ?>>Mes <?= $m ?></option>
            <?php endfor; ?>
        </select>
        <button type="submit" style="padding: 5px;">Buscar</button>
        <a href="?route=documentos_por_pagar/index" style="margin-left: 10px;">Limpiar</a>
    </form>
    <a href="?route=documentos_por_pagar/form" style="background-color: #28a745; color: white; padding: 10px 15px; text-decoration: none; border-radius: 4px;">+ Recep. de Documento</a>
</div>

<table border="1" cellpadding="8" cellspacing="0" style="width: 100%; border-collapse: collapse;">
    <thead style="background-color: #0056b3; color: white;">
        <tr>
            <th>Documento</th>
            <th>Emisión</th>
            <th>Proveedor</th>
            <th>Tipo</th>
            <th>Concepto / Observ.</th>
            <th>Total</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php if (empty($documentos)): ?>
            <tr>
                <td colspan="7" style="text-align: center;">No se encontraron documentos por pagar.</td>
            </tr>
        <?php else: ?>
            <?php foreach ($documentos as $data): ?>
                <?php 
                    $doc = $data['entity']; 
                    $proveedor = $data['proveedor'];
                    $tipo = $data['tipo_documento'];
                ?>
                <tr>
                    <td><?= htmlspecialchars($doc->nroDocumento) ?></td>
                    <td><?= htmlspecialchars($doc->fechaEmision) ?></td>
                    <td><?= htmlspecialchars($proveedor) ?></td>
                    <td><?= htmlspecialchars($tipo) ?></td>
                    <td><?= htmlspecialchars($doc->observacion) ?></td>
                    <td><?= number_format($doc->montoTotal, 2, ',', '.') ?></td>
                    <td>
                        <a href="?route=documental/documentoCxP&id=<?= urlencode((string)$doc->id) ?>" target="_blank" style="margin-right: 10px; color: #198754; font-weight: bold;">[ PDF ]</a>
                        <a href="?route=documentos_por_pagar/form&id=<?= urlencode((string)$doc->id) ?>" style="margin-right: 10px;">Editar</a>
                        <form method="POST" action="?route=documentos_por_pagar/eliminar" onsubmit="return confirm('¿Seguro de eliminar este documento?');" style="display:inline;">
                            <input type="hidden" name="id" value="<?= htmlspecialchars((string)$doc->id) ?>">
                            <button type="submit" style="color: red; border:none; background:none; cursor:pointer;">Eliminar</button>
                        </form>
                    </td>
                </tr>
            <?php endforeach; ?>
        <?php endif; ?>
    </tbody>
</table>
