<div class="card card-outline card-primary">
  <div class="card-header">
    <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
    <div class="card-tools">
      <a href="?route=documentos_por_pagar/form" class="btn btn-sm btn-success">
        <i class="fas fa-plus"></i> Recep. de Documento
      </a>
    </div>
  </div>
  <div class="card-body">
    <?php if (isset($error)): ?>
      <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
    <?php endif; ?>

    <form method="GET" action="" class="form-inline mb-4">
      <input type="hidden" name="route" value="documentos_por_pagar/index">
      <div class="input-group mr-2">
        <input type="text" class="form-control" name="search" placeholder="Buscar CxP..." value="<?= htmlspecialchars($search) ?>" style="width: 250px;">
        <select class="form-control" name="mes">
          <option value="">Todos los Meses</option>
          <?php for($i=1; $i<=12; $i++): $m = str_pad((string)$i, 2, '0', STR_PAD_LEFT); ?>
            <option value="<?= htmlspecialchars((string)$m) ?>" <?= ($mes === $m) ? 'selected' : '' ?>>Mes <?= htmlspecialchars((string)$m) ?></option>
          <?php endfor; ?>
        </select>
        <span class="input-group-append">
          <button type="submit" class="btn btn-primary"><i class="fas fa-search"></i> Buscar</button>
        </span>
      </div>
      <a href="?route=documentos_por_pagar/index" class="btn btn-default">Limpiar</a>
    </form>

    <div class="table-responsive">
      <table class="table table-bordered table-striped w-100 table-sm">
        <thead class="thead-dark">
          <tr>
            <th>Documento</th>
            <th>Emisión</th>
            <th>Proveedor</th>
            <th>Tipo</th>
            <th>Concepto / Observ.</th>
            <th class="text-right">Total</th>
            <th class="text-center">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <?php if (empty($documentos)): ?>
            <tr><td colspan="7" class="text-center">No se encontraron documentos por pagar.</td></tr>
          <?php else: ?>
            <?php foreach ($documentos as $data): ?>
              <?php 
                $doc = $data['entity']; 
                $proveedor = $data['proveedor'];
                $tipo = $data['tipo_documento'];
              ?>
              <tr>
                <td><strong><?= htmlspecialchars($doc->nroDocumento) ?></strong></td>
                <td><?= htmlspecialchars($doc->fechaEmision) ?></td>
                <td><?= htmlspecialchars($proveedor) ?></td>
                <td><?= htmlspecialchars($tipo) ?></td>
                <td><?= htmlspecialchars($doc->observacion) ?></td>
                <td class="text-right font-weight-bold"><?= number_format($doc->montoTotal, 2, ',', '.') ?></td>
                <td class="text-center text-nowrap">
                  <a href="?route=documental/documentoCxP&id=<?= urlencode((string)$doc->id) ?>" target="_blank" class="btn btn-sm btn-success" title="Ver PDF">
                    <i class="fas fa-file-pdf"></i>
                  </a>
                  <?= render_component('table_actions', [
                'editUrl' => '?route=documentos_por_pagar/form&id=' . $doc->id,
                'deleteUrl' => '?route=documentos_por_pagar/eliminar',
                'deleteId' => $doc->id
            ]) ?>
                </td>
              </tr>
            <?php endforeach; ?>
          <?php endif; ?>
        </tbody>
      </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
    </div>
  </div>
</div>
