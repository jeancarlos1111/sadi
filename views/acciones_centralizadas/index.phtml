<?php if (isset($error)): ?>
  <?= render_component('alert', ['type' => 'danger', 'message' => $error]) ?>
<?php endif; ?>

<div class="card card-primary card-outline">
  <?= render_component('page_header', [
        'title' => $titulo,
        'icon' => 'fas fa-landmark',
        'newUrl' => '?route=acciones_centralizadas/form',
        'newText' => 'Nueva Acción Centralizada'
      ]) ?>
  <div class="card-body">
    <?= render_component('search_filter', [
        'route' => 'acciones_centralizadas/index',
        'searchValue' => $search ?? '',
        'placeholder' => 'Buscar por código...'
      ]) ?>

    <table class="table table-bordered table-striped w-100">
      <thead class="thead-dark">
        <tr>
          <th>Código A.C.</th>
          <th>Denominación</th>
          <th>Unidad Medida</th>
          <th class="text-center">Año Inicio</th>
          <th class="text-center">Año Culminación</th>
          <th class="text-center" width="150">Acciones</th>
        </tr>
      </thead>
      <tbody>
        <?php if (!empty($items)): ?>
          <?php foreach ($items as $item): ?>
            <tr>
              <td class="align-middle"><strong><?= htmlspecialchars($item->codigo_accion_centralizada) ?></strong></td>
              <td class="align-middle"><?= htmlspecialchars($item->denominacion) ?></td>
              <td class="align-middle"><?= htmlspecialchars($item->unidad_medida) ?></td>
              <td class="text-center align-middle"><?= htmlspecialchars($item->anio_inicio) ?></td>
              <td class="text-center align-middle"><?= htmlspecialchars($item->anio_culm) ?></td>
              <td class="text-center align-middle">
                <?= render_component('table_actions', [
                'editUrl' => '?route=acciones_centralizadas/form&id=' . $item->id_accion_centralizada,
                'deleteUrl' => '?route=acciones_centralizadas/eliminar',
                'deleteId' => $item->id_accion_centralizada
            ]) ?>
              </td>
            </tr>
          <?php endforeach; ?>
        <?php endif; ?>
      </tbody>
    </table>
    <?php if (isset($paginator)): ?>
      <?php include $partialsPath . 'pagination.phtml'; ?>
    <?php endif; ?>
  </div>
</div>
