<div class="row">
    <div class="col-md-10 offset-md-1">
        <div class="card card-outline card-primary">
            <div class="card-header">
                <h3 class="card-title"><?= htmlspecialchars($titulo) ?></h3>
            </div>
            
            <form method="POST" action="?route=acciones_centralizadas/guardar">
                <div class="card-body">
                    <?php if (isset($error)): ?>
                        <div class="alert alert-danger alert-dismissible">
                            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
                            <h5><i class="icon fas fa-ban"></i> Error</h5>
                            <?= htmlspecialchars($error) ?>
                        </div>
                    <?php endif; ?>

                    <?php if (isset($item) && $item->id_accion_centralizada): ?>
                        <input type="hidden" name="id" value="<?= htmlspecialchars((string)$item->id_accion_centralizada) ?>">
                    <?php endif; ?>

                    <h5 class="text-primary border-bottom pb-2 mb-3">Datos Generales de la Acción Centralizada</h5>
                    <div class="row">
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Código A.C. <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="codigo_accion_centralizada" value="<?= htmlspecialchars($item->codigo_accion_centralizada ?? '') ?>" required maxlength="50">
                            </div>
                        </div>
                        <div class="col-md-6">
                            <div class="form-group">
                                <label>Denominación <span class="text-danger">*</span></label>
                                <input type="text" class="form-control" name="denominacion" value="<?= htmlspecialchars($item->denominacion ?? '') ?>" required maxlength="255">
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Unidad de Medida:</label>
                                <input type="text" class="form-control" name="unidad_medida" value="<?= htmlspecialchars($item->unidad_medida ?? '') ?>" maxlength="50">
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Año de Inicio:</label>
                                <input type="text" class="form-control" name="anio_inicio" value="<?= htmlspecialchars($item->anio_inicio ?? '') ?>">
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Año de Culminación:</label>
                                <input type="text" class="form-control" name="anio_culm" value="<?= htmlspecialchars($item->anio_culm ?? '') ?>">
                            <div class="row">
                                <div class="col-md-12 form-group">
                                    <label>Unidad Ejecutora / Administrativa</label>
                                    <select class="form-control" name="id_unidad_administrativa">
                                        <option value="">-- Seleccione --</option>
                                        <?php foreach ($unidades as $u): ?>
                                            <option value="<?= htmlspecialchars((string)$u->id_unidad_administrativa) ?>" <?= ($item && $item->id_unidad_administrativa == $u->id_unidad_administrativa) ? 'selected' : '' ?>>
                                                <?= htmlspecialchars($u->codigo . ' - ' . $u->denominacion) ?>
                                            </option>
                                        <?php endforeach; ?>
                                    </select>
                                </div>
                            </div>
                        </div>
                    </div>

                    <div class="card card-warning">
                        <div class="card-header d-flex justify-content-between align-items-center">
                            <h3 class="card-title">Plan Operativo Anual (POA) - Indicadores Múltiples</h3>
                            <button type="button" class="btn btn-sm btn-dark ml-auto" id="btn-add-indicador"><i class="fas fa-plus"></i> Añadir Indicador</button>
                        </div>
                        <div class="card-body p-0">
                            <table class="table table-bordered table-striped" id="tabla-indicadores">
                                <thead>
                                    <tr>
                                        <th>Eficacia</th>
                                        <th>Eficiencia</th>
                                        <th>Calidad</th>
                                        <th>Impacto</th>
                                        <th>Medio de Verificación</th>
                                        <th style="width: 50px;">Acción</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <?php if(!empty($indicadores)): ?>
                                        <?php foreach($indicadores as $ind): ?>
                                            <tr>
                                                <td><textarea class="form-control" name="ind_eficacia[]" rows="2"><?= htmlspecialchars($ind->indicador_eficacia ?? '') ?></textarea></td>
                                                <td><textarea class="form-control" name="ind_eficiencia[]" rows="2"><?= htmlspecialchars($ind->indicador_eficiencia ?? '') ?></textarea></td>
                                                <td><textarea class="form-control" name="ind_calidad[]" rows="2"><?= htmlspecialchars($ind->indicador_calidad ?? '') ?></textarea></td>
                                                <td><textarea class="form-control" name="ind_impacto[]" rows="2"><?= htmlspecialchars($ind->indicador_impacto ?? '') ?></textarea></td>
                                                <td><textarea class="form-control" name="ind_medio[]" rows="2"><?= htmlspecialchars($ind->medio_verificacion ?? '') ?></textarea></td>
                                                <td><button type="button" class="btn btn-danger btn-sm btn-remove-ind"><i class="fas fa-trash"></i></button></td>
                                            </tr>
                                        <?php endforeach; ?>
                                    <?php else: ?>
                                        <tr>
                                            <td><textarea class="form-control" name="ind_eficacia[]" rows="2"><?= htmlspecialchars($item->indicador_eficacia ?? '') ?></textarea></td>
                                            <td><textarea class="form-control" name="ind_eficiencia[]" rows="2"><?= htmlspecialchars($item->indicador_eficiencia ?? '') ?></textarea></td>
                                            <td><textarea class="form-control" name="ind_calidad[]" rows="2"><?= htmlspecialchars($item->indicador_calidad ?? '') ?></textarea></td>
                                            <td><textarea class="form-control" name="ind_impacto[]" rows="2"><?= htmlspecialchars($item->indicador_impacto ?? '') ?></textarea></td>
                                            <td><textarea class="form-control" name="ind_medio[]" rows="2"><?= htmlspecialchars($item->medio_verificacion ?? '') ?></textarea></td>
                                            <td><button type="button" class="btn btn-danger btn-sm btn-remove-ind"><i class="fas fa-trash"></i></button></td>
                                        </tr>
                                    <?php endif; ?>
                                </tbody>
                            </table>
                        </div>
                    </div>

                    <h5 class="text-primary border-bottom pb-2 mt-4 mb-3">Metas Trimestrales (Cantidades)</h5>
                    <div class="table-responsive">
                        <table class="table table-bordered table-striped w-100 text-center">
                            <thead class="thead-light">
                                <tr>
                                    <th>Trimestre</th>
                                    <th>Programado</th>
                                    <th>Ejecutado</th>
                                </tr>
                            </thead>
                            <tbody>
                                <?php
                                $trimestres = [
                                    'i' => 'I Trimestre',
                                    'ii' => 'II Trimestre',
                                    'iii' => 'III Trimestre',
                                    'iv' => 'IV Trimestre',
                                ];
                                foreach ($trimestres as $t => $label):
                                ?>
                                <tr>
                                    <td class="font-weight-bold align-middle"><?= htmlspecialchars((string)$label) ?></td>
                                    <td><input type="number" class="form-control" step="0.01" name="cant_programada_trim_<?= htmlspecialchars((string)$t) ?>" value="<?= htmlspecialchars((string)($item->{'cant_programada_trim_'.$t} ?? 0)) ?>"></td>
                                    <td><input type="number" class="form-control" step="0.01" name="cant_ejecutada_trim_<?= htmlspecialchars((string)$t) ?>" value="<?= htmlspecialchars((string)($item->{'cant_ejecutada_trim_'.$t} ?? 0)) ?>"></td>
                                </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
                <div class="card-footer text-right">
                    <button type="submit" class="btn btn-success"><i class="fas fa-save"></i> Guardar A.C. y Metas</button>
                    <a href="?route=acciones_centralizadas/index" class="btn btn-default"><i class="fas fa-times"></i> Cancelar</a>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const btnAdd = document.getElementById('btn-add-indicador');
    const tbody = document.querySelector('#tabla-indicadores tbody');

    btnAdd.addEventListener('click', function() {
        const tr = document.createElement('tr');
        tr.innerHTML = `
            <td><textarea class="form-control" name="ind_eficacia[]" rows="2"></textarea></td>
            <td><textarea class="form-control" name="ind_eficiencia[]" rows="2"></textarea></td>
            <td><textarea class="form-control" name="ind_calidad[]" rows="2"></textarea></td>
            <td><textarea class="form-control" name="ind_impacto[]" rows="2"></textarea></td>
            <td><textarea class="form-control" name="ind_medio[]" rows="2"></textarea></td>
            <td><button type="button" class="btn btn-danger btn-sm btn-remove-ind"><i class="fas fa-trash"></i></button></td>
        `;
        tbody.appendChild(tr);
    });

    tbody.addEventListener('click', function(e) {
        if (e.target.closest('.btn-remove-ind')) {
            const row = e.target.closest('tr');
            if (tbody.querySelectorAll('tr').length > 1) {
                row.remove();
            } else {
                alert('Debe mantener al menos un indicador');
            }
        }
    });
});
</script>
