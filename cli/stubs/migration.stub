-- Migración: {{ TableName }}
-- Generada: {{ Date }}

CREATE TABLE IF NOT EXISTS {{ TableName }} (
    id_{{ TableName }} SERIAL PRIMARY KEY,
    -- TODO: agregar columnas
    eliminado BOOLEAN DEFAULT false
);
